---
phase: 08-security-pipeline-worktree-manager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/codebot/security/__init__.py
  - apps/server/src/codebot/security/models.py
  - apps/server/src/codebot/security/scanners/__init__.py
  - apps/server/src/codebot/security/scanners/base.py
  - apps/server/src/codebot/security/scanners/semgrep.py
  - apps/server/src/codebot/security/scanners/trivy.py
  - apps/server/src/codebot/security/scanners/gitleaks.py
  - apps/server/src/codebot/security/scanners/allowlist.py
  - apps/server/src/codebot/security/gate.py
  - apps/server/src/codebot/security/config.py
  - configs/security/thresholds.yaml
  - configs/security/allowlist.yaml
  - configs/security/gitleaks.toml
  - apps/server/tests/unit/test_security_semgrep.py
  - apps/server/tests/unit/test_security_trivy.py
  - apps/server/tests/unit/test_security_gitleaks.py
  - apps/server/tests/unit/test_security_gate.py
  - apps/server/tests/unit/test_allowlist.py
autonomous: true
requirements:
  - SECP-01
  - SECP-02
  - SECP-03
  - SECP-04
  - SECP-06
  - CMPL-04

must_haves:
  truths:
    - "Semgrep scanner adapter parses JSON output into normalized SecurityFinding list"
    - "Trivy scanner adapter parses JSON output into normalized SecurityFinding list"
    - "Gitleaks scanner adapter parses JSON output into normalized SecurityFinding list with CRITICAL severity for all secrets"
    - "Security gate blocks when critical or high findings exceed configured thresholds"
    - "Allowlist validator rejects packages not in the approved list for both pip and npm"
  artifacts:
    - path: "apps/server/src/codebot/security/models.py"
      provides: "ScanResult, SecurityReport, SecurityThresholds, GateResult, AllowlistConfig Pydantic models"
      exports: ["ScanResult", "SecurityReport", "SecurityThresholds", "GateResult", "AllowlistConfig"]
    - path: "apps/server/src/codebot/security/scanners/base.py"
      provides: "BaseScanner ABC with async scan() and _run_cli()"
      exports: ["BaseScanner"]
    - path: "apps/server/src/codebot/security/scanners/semgrep.py"
      provides: "SASTRunner adapter for Semgrep CLI"
      exports: ["SASTRunner"]
    - path: "apps/server/src/codebot/security/scanners/trivy.py"
      provides: "DependencyScanner adapter for Trivy CLI"
      exports: ["DependencyScanner"]
    - path: "apps/server/src/codebot/security/scanners/gitleaks.py"
      provides: "SecretScanner adapter for Gitleaks CLI"
      exports: ["SecretScanner"]
    - path: "apps/server/src/codebot/security/scanners/allowlist.py"
      provides: "AllowlistValidator for pip/npm dependency checking"
      exports: ["AllowlistValidator"]
    - path: "apps/server/src/codebot/security/gate.py"
      provides: "SecurityGate with threshold-based pass/fail evaluation"
      exports: ["SecurityGate"]
  key_links:
    - from: "apps/server/src/codebot/security/scanners/semgrep.py"
      to: "apps/server/src/codebot/security/models.py"
      via: "ScanResult and ScanFinding imports"
      pattern: "from codebot\\.security\\.models import"
    - from: "apps/server/src/codebot/security/gate.py"
      to: "apps/server/src/codebot/security/models.py"
      via: "SecurityReport and GateResult imports"
      pattern: "from codebot\\.security\\.models import"
    - from: "apps/server/src/codebot/security/scanners/base.py"
      to: "asyncio.create_subprocess_exec"
      via: "_run_cli method"
      pattern: "asyncio\\.create_subprocess_exec"
---

<objective>
Build all security scanner adapters (Semgrep, Trivy, Gitleaks), the security quality gate, the dependency allowlist validator, and their shared Pydantic models with full unit test coverage.

Purpose: Provides the scanning and evaluation foundation that the SecurityOrchestrator (Plan 03) will coordinate. Each scanner wraps a CLI tool behind an async adapter that normalizes JSON output into shared SecurityFinding structures.

Output: Security scanner package with 3 scanner adapters, gate evaluator, allowlist validator, configuration files, and unit tests.
</objective>

<execution_context>
@/Users/v13478/.claude/get-shit-done/workflows/execute-plan.md
@/Users/v13478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-security-pipeline-worktree-manager/08-RESEARCH.md

<interfaces>
<!-- Existing ORM models from Phase 1 that scanners map findings INTO -->

From apps/server/src/codebot/db/models/security.py:
```python
class FindingType(enum.Enum):
    VULNERABILITY = "VULNERABILITY"
    SECRET = "SECRET"
    LICENSE_VIOLATION = "LICENSE_VIOLATION"
    CODE_SMELL = "CODE_SMELL"
    DEPENDENCY_RISK = "DEPENDENCY_RISK"
    CONFIG_ISSUE = "CONFIG_ISSUE"

class Severity(enum.Enum):
    CRITICAL = "CRITICAL"
    HIGH = "HIGH"
    MEDIUM = "MEDIUM"
    LOW = "LOW"
    INFO = "INFO"

class FindingStatus(enum.Enum):
    OPEN = "OPEN"
    IN_PROGRESS = "IN_PROGRESS"
    FIXED = "FIXED"
    ACCEPTED_RISK = "ACCEPTED_RISK"
    FALSE_POSITIVE = "FALSE_POSITIVE"

class SecurityFinding(Base):
    __tablename__ = "security_findings"
    # Has: id, project_id, scanner, finding_type, severity, title, description,
    #      file_path, line_number, code_snippet, cwe_id, cve_id, recommendation,
    #      status, fixed_by_agent_id, fixed_at, created_at
```

From apps/server/src/codebot/config.py:
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="CODEBOT_", ...)
    database_url: str = "postgresql+asyncpg://codebot:codebot_dev@localhost:5433/codebot"
    redis_url: str = "redis://localhost:6379/0"
    nats_url: str = "nats://localhost:4222"
```

From apps/server/src/codebot/db/models/base.py:
```python
class Base(DeclarativeBase):
    type_annotation_map: dict[type, Any] = {
        uuid.UUID: sa.Uuid,
        datetime: sa.DateTime(timezone=True),
    }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Security Pydantic models, BaseScanner ABC, SecurityGate, and config files</name>
  <files>
    apps/server/src/codebot/security/__init__.py
    apps/server/src/codebot/security/models.py
    apps/server/src/codebot/security/scanners/__init__.py
    apps/server/src/codebot/security/scanners/base.py
    apps/server/src/codebot/security/gate.py
    apps/server/src/codebot/security/config.py
    configs/security/thresholds.yaml
    configs/security/allowlist.yaml
    configs/security/gitleaks.toml
    apps/server/tests/unit/test_security_gate.py
  </files>
  <read_first>
    apps/server/src/codebot/db/models/security.py
    apps/server/src/codebot/db/models/base.py
    apps/server/src/codebot/config.py
    apps/server/src/codebot/__init__.py
    .planning/phases/08-security-pipeline-worktree-manager/08-RESEARCH.md
  </read_first>
  <behavior>
    - Test: ScanResult can be instantiated with scanner name, findings list, errors list, duration_ms
    - Test: SecurityThresholds defaults are max_critical=0, max_high=0, max_medium=5, max_low=20, require_no_secrets=True
    - Test: GateResult.passed is False when critical_count > 0 and max_critical=0
    - Test: GateResult.passed is False when high_count > 0 and max_high=0
    - Test: GateResult.passed is True when only medium findings within threshold
    - Test: GateResult.passed is False when secrets detected and require_no_secrets=True
    - Test: GateResult.warnings populated when medium_count exceeds max_medium
    - Test: AllowlistConfig can hold python_packages and npm_packages sets
    - Test: BaseScanner._run_cli raises asyncio.TimeoutError after timeout
  </behavior>
  <action>
    Create the security package directory structure:

    1. `apps/server/src/codebot/security/__init__.py`:
       Export key classes: `from codebot.security.models import ScanResult, SecurityReport, SecurityThresholds, GateResult, AllowlistConfig`
       Export: `from codebot.security.gate import SecurityGate`
       Export: `from codebot.security.scanners.base import BaseScanner`

    2. `apps/server/src/codebot/security/models.py`:
       Import `Severity` from `codebot.db.models.security` (reuse existing enum, do NOT redefine).
       Import `FindingType` from `codebot.db.models.security`.

       Define these Pydantic v2 models (use `from pydantic import BaseModel, Field`):

       `class ScanFinding(BaseModel)` with fields: tool (str), rule_id (str, default ""), severity (Severity), title (str), description (str, default ""), file_path (str, default ""), line_start (int, default 0), line_end (int, default 0), code_snippet (str, default ""), cwe (list[str], default_factory=list), cve_id (str | None, default None), fix_recommendation (str, default "")

       `class ScanResult(BaseModel)` with fields: scanner (str), findings (list[ScanFinding], default_factory=list), errors (list[str], default_factory=list), duration_ms (int, default 0)

       `class ScanSummary(BaseModel)` with fields: total_findings (int, default 0), critical_count (int, default 0), high_count (int, default 0), medium_count (int, default 0), low_count (int, default 0), info_count (int, default 0), secrets_count (int, default 0)

       `class GateResult(BaseModel)` with fields: passed (bool), reason (str, default ""), warnings (list[str], default_factory=list)

       `class SecurityThresholds(BaseModel)` with fields: max_critical (int, default 0), max_high (int, default 0), max_medium (int, default 5), max_low (int, default 20), require_no_secrets (bool, default True), require_compliance_pass (bool, default False)

       `class ScanError(BaseModel)` with fields: scanner (str), error (str)

       `class SecurityReport(BaseModel)` with fields: findings (list[ScanFinding], default_factory=list), errors (list[ScanError], default_factory=list), summary (ScanSummary, default_factory=ScanSummary), gate_result (GateResult | None, default None), compliance_report (ComplianceReport | None, default None)

       `class AllowlistConfig(BaseModel)` with fields: python_packages (set[str], default_factory=set), npm_packages (set[str], default_factory=set), require_hashes (bool, default True), block_unknown (bool, default True)

    3. `apps/server/src/codebot/security/scanners/__init__.py`: Empty init file.

    4. `apps/server/src/codebot/security/scanners/base.py`:
       ABC with abstract `async def scan(self, project_path: str) -> ScanResult`
       Helper `async def _run_cli(self, cmd: list[str], timeout: int = 300) -> tuple[str, str, int]`:
       Uses `asyncio.create_subprocess_exec(*cmd, stdout=PIPE, stderr=PIPE)`, then `asyncio.wait_for(proc.communicate(), timeout=timeout)`.
       On TimeoutError: kill process, re-raise.
       Returns (stdout_decoded, stderr_decoded, returncode).

    5. `apps/server/src/codebot/security/gate.py`:
       `class SecurityGate` with `__init__(self, thresholds: SecurityThresholds | None = None)`.
       `def evaluate(self, report: SecurityReport) -> GateResult`:
       Check secrets_count > 0 and require_no_secrets -> fail with "Hardcoded secrets detected".
       Check critical_count > max_critical -> fail with "CRITICAL findings (N) exceed threshold (M)".
       Check high_count > max_high -> fail with "HIGH findings (N) exceed threshold (M)".
       Check medium > max_medium -> add warning (but still pass).
       Check low > max_low -> add warning (but still pass).
       If none fail -> GateResult(passed=True, warnings=warnings).

    6. `apps/server/src/codebot/security/config.py`:
       Functions: `load_thresholds(path: str = "configs/security/thresholds.yaml") -> SecurityThresholds` and `load_allowlist(path: str = "configs/security/allowlist.yaml") -> AllowlistConfig`.
       Use `import yaml` (PyYAML) to load YAML, then construct Pydantic models.
       Constants: `THRESHOLDS_PATH`, `ALLOWLIST_PATH`, `GITLEAKS_CONFIG_PATH`.

    7. `configs/security/thresholds.yaml`: max_critical: 0, max_high: 0, max_medium: 5, max_low: 20, require_no_secrets: true, require_compliance_pass: false

    8. `configs/security/allowlist.yaml`: python_packages list (fastapi, pydantic, sqlalchemy, uvicorn, httpx, pytest, ruff, alembic, nats-py, gitpython, semgrep, ephemeral-port-reserve, pydantic-settings, asyncpg, redis, pyyaml) and npm_packages list (react, react-dom, typescript, vite, tailwindcss, zustand, @tanstack/react-query, vitest). require_hashes: true, block_unknown: true.

    9. `configs/security/gitleaks.toml`: title, [allowlist] section with paths (tests/.*, fixtures/.*, .env.example) and regexes (codebot_dev_key, test_secret_value, PLACEHOLDER_.*).

    10. `apps/server/tests/unit/test_security_gate.py`: Tests covering gate pass/fail/warn scenarios:
        test_gate_passes_no_findings, test_gate_fails_critical, test_gate_fails_high, test_gate_fails_secrets, test_gate_passes_medium_within_threshold, test_gate_warns_medium_above_threshold, test_custom_thresholds.
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run pytest apps/server/tests/unit/test_security_gate.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - apps/server/src/codebot/security/models.py contains `class ScanFinding(BaseModel):`
    - apps/server/src/codebot/security/models.py contains `class ScanResult(BaseModel):`
    - apps/server/src/codebot/security/models.py contains `class SecurityThresholds(BaseModel):`
    - apps/server/src/codebot/security/models.py contains `class GateResult(BaseModel):`
    - apps/server/src/codebot/security/models.py contains `class AllowlistConfig(BaseModel):`
    - apps/server/src/codebot/security/models.py contains `from codebot.db.models.security import Severity`
    - apps/server/src/codebot/security/scanners/base.py contains `class BaseScanner(ABC):`
    - apps/server/src/codebot/security/scanners/base.py contains `async def _run_cli(`
    - apps/server/src/codebot/security/scanners/base.py contains `asyncio.create_subprocess_exec`
    - apps/server/src/codebot/security/gate.py contains `class SecurityGate:`
    - apps/server/src/codebot/security/gate.py contains `def evaluate(self, report: SecurityReport) -> GateResult:`
    - configs/security/thresholds.yaml contains `max_critical: 0`
    - configs/security/allowlist.yaml contains `python_packages:`
    - configs/security/gitleaks.toml contains `[allowlist]`
    - apps/server/tests/unit/test_security_gate.py exits 0
  </acceptance_criteria>
  <done>Security models, BaseScanner ABC, SecurityGate, config loaders, and YAML config files exist. Gate unit tests pass covering pass/fail/warn scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Semgrep, Trivy, Gitleaks scanner adapters and AllowlistValidator with unit tests</name>
  <files>
    apps/server/src/codebot/security/scanners/semgrep.py
    apps/server/src/codebot/security/scanners/trivy.py
    apps/server/src/codebot/security/scanners/gitleaks.py
    apps/server/src/codebot/security/scanners/allowlist.py
    apps/server/tests/unit/test_security_semgrep.py
    apps/server/tests/unit/test_security_trivy.py
    apps/server/tests/unit/test_security_gitleaks.py
    apps/server/tests/unit/test_allowlist.py
  </files>
  <read_first>
    apps/server/src/codebot/security/models.py
    apps/server/src/codebot/security/scanners/base.py
    apps/server/src/codebot/security/gate.py
    apps/server/src/codebot/db/models/security.py
    .planning/phases/08-security-pipeline-worktree-manager/08-RESEARCH.md
  </read_first>
  <behavior>
    - Test: SASTRunner.scan() parses Semgrep JSON with 2 results into 2 ScanFindings with correct severity mapping (ERROR->HIGH, WARNING->MEDIUM, INFO->LOW)
    - Test: SASTRunner handles exit code 0 (no findings) returning empty findings list
    - Test: SASTRunner handles exit code 1 (findings found) and parses JSON
    - Test: SASTRunner handles exit code >= 2 (error) returning errors list
    - Test: DependencyScanner.scan() parses Trivy JSON with Results[].Vulnerabilities array into findings
    - Test: DependencyScanner maps Trivy severity strings (CRITICAL, HIGH, MEDIUM) to Severity enum
    - Test: DependencyScanner includes fix_recommendation with FixedVersion when available
    - Test: SecretScanner.scan() parses Gitleaks JSON array into findings with Severity.CRITICAL
    - Test: SecretScanner handles exit code 0 (no leaks) returning empty findings
    - Test: SecretScanner handles exit code 1 (leaks found) and parses JSON
    - Test: SecretScanner truncates code_snippet to 200 characters
    - Test: AllowlistValidator.validate_requirements rejects unknown packages
    - Test: AllowlistValidator.validate_requirements allows listed packages
    - Test: AllowlistValidator.validate_package_json rejects unknown npm packages
    - Test: AllowlistValidator.validate_package_json allows listed npm packages
  </behavior>
  <action>
    1. `apps/server/src/codebot/security/scanners/semgrep.py` -- Create `SASTRunner(BaseScanner)`:
       - Class attribute: `DEFAULT_CONFIGS = ["p/security-audit", "p/owasp-top-ten", "p/cwe-top-25"]`
       - `__init__(self, custom_rules_dir: str | None = None)` storing custom_rules_dir
       - `async def scan(self, project_path: str) -> ScanResult`: Build command `["semgrep", "scan", "--json", "--no-git-ignore"]` plus `--config` for each DEFAULT_CONFIGS item plus custom_rules_dir if set, then project_path. Call `self._run_cli(cmd, timeout=300)`. If returncode >= 2: return ScanResult with errors. If returncode 0 or 1: parse JSON stdout, extract from `data["results"]` each item's check_id, extra.severity, extra.message, path, start.line, end.line, extra.lines (truncated), extra.metadata.cwe (list), extra.fix. Map severity via `_map_severity`.
       - `def _map_severity(self, semgrep_severity: str) -> Severity`: Map "ERROR"->Severity.HIGH, "WARNING"->Severity.MEDIUM, "INFO"->Severity.LOW. Default: Severity.INFO.

    2. `apps/server/src/codebot/security/scanners/trivy.py` -- Create `DependencyScanner(BaseScanner)`:
       - `async def scan(self, project_path: str) -> ScanResult`: Command: `["trivy", "filesystem", "--format", "json", "--scanners", "vuln,secret,misconfig", "--severity", "CRITICAL,HIGH,MEDIUM", "--skip-dirs", "node_modules,.git,.worktrees", project_path]`. Call `self._run_cli(cmd, timeout=600)`. If returncode != 0 and no stdout: return errors. Parse JSON, iterate `data.get("Results", [])`, then `target.get("Vulnerabilities", [])` (handle None since Trivy returns null for no vulns). Map each vuln: VulnerabilityID as rule_id, Severity via `Severity(vuln["Severity"].upper())`, Title (include PkgName if present), Description, Target as file_path, FixedVersion in fix_recommendation as `f"Update to {fixed_version}"`.

    3. `apps/server/src/codebot/security/scanners/gitleaks.py` -- Create `SecretScanner(BaseScanner)`:
       - `__init__(self, config_path: str | None = None)` storing config_path
       - `async def scan(self, project_path: str) -> ScanResult`: Command: `["gitleaks", "detect", "--source", project_path, "--report-format", "json", "--report-path", "/dev/stdout", "--no-git", "--no-banner"]`. If config_path: extend with `["--config", self.config_path]`. Call `self._run_cli(cmd, timeout=120)`. If returncode == 0: return ScanResult with empty findings. If returncode == 1 and stdout non-empty: parse JSON array, map each leak to ScanFinding with rule_id=leak["RuleID"], severity=Severity.CRITICAL always, title=f"Secret detected: {leak['Description']}", file_path=leak["File"], line_start=leak.get("StartLine", 0), line_end=leak.get("EndLine", 0), code_snippet=leak.get("Match", "")[:200], fix_recommendation="Remove hardcoded secret. Use environment variables or a secrets manager instead." If returncode > 1: return ScanResult with errors.

    4. `apps/server/src/codebot/security/scanners/allowlist.py` -- Create `AllowlistValidator`:
       - `__init__(self, config: AllowlistConfig)` storing config
       - `async def validate_requirements(self, requirements_path: str) -> list[str]`: Read file line by line. For each line, call `_extract_package_name(line)`. If pkg_name is not None and `pkg_name.lower()` not in `{p.lower() for p in self.config.python_packages}`: append `f"Package '{pkg_name}' not in Python allowlist"` to violations. Return violations list.
       - `async def validate_package_json(self, pkg_json_path: str) -> list[str]`: Read JSON via `json.load`. For sections "dependencies" and "devDependencies", check each key against npm_packages. Return violations.
       - `def _extract_package_name(self, line: str) -> str | None`: Strip whitespace. Skip empty, comments starting with `#`, lines starting with `-r` or `--`. Split on first occurrence of any of `==`, `>=`, `<=`, `~=`, `!=`, `<`, `>`, `[`, `;`, ` `. Return stripped first part, or None if empty.

    5. Write unit tests that mock `_run_cli` via `unittest.mock.AsyncMock`:

       `test_security_semgrep.py`: Create SEMGREP_JSON_OUTPUT fixture with 2 results (one ERROR severity, one WARNING). Test: mock `SASTRunner._run_cli` returning this fixture with returncode 1 -> verify 2 findings with HIGH and MEDIUM severity. Test no-findings (returncode 0, empty results). Test error (returncode 2). Test _map_severity for all cases.

       `test_security_trivy.py`: Create TRIVY_JSON_OUTPUT fixture with Results containing Vulnerabilities array (2 vulns, one CRITICAL, one HIGH). Test: mock `DependencyScanner._run_cli` -> verify findings count, severity mapping, fix_recommendation contains FixedVersion. Test empty Results. Test error case.

       `test_security_gitleaks.py`: Create GITLEAKS_JSON_OUTPUT fixture (JSON array with 2 leak objects containing RuleID, Description, File, StartLine, EndLine, Match). Test: mock returncode 1 -> verify 2 findings all CRITICAL. Test returncode 0 -> empty. Test returncode > 1 -> errors. Test Match > 200 chars is truncated.

       `test_allowlist.py`: Use `tmp_path` fixture to create temp requirements.txt and package.json files. Test allowed packages produce empty violations. Test unknown packages produce violation strings. Test _extract_package_name with version specifiers, comments, blank lines.
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run pytest apps/server/tests/unit/test_security_semgrep.py apps/server/tests/unit/test_security_trivy.py apps/server/tests/unit/test_security_gitleaks.py apps/server/tests/unit/test_allowlist.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - apps/server/src/codebot/security/scanners/semgrep.py contains `class SASTRunner(BaseScanner):`
    - apps/server/src/codebot/security/scanners/semgrep.py contains `"p/security-audit"` and `"p/owasp-top-ten"` and `"p/cwe-top-25"`
    - apps/server/src/codebot/security/scanners/semgrep.py contains `async def scan(self, project_path: str) -> ScanResult:`
    - apps/server/src/codebot/security/scanners/trivy.py contains `class DependencyScanner(BaseScanner):`
    - apps/server/src/codebot/security/scanners/trivy.py contains `"trivy", "filesystem"`
    - apps/server/src/codebot/security/scanners/gitleaks.py contains `class SecretScanner(BaseScanner):`
    - apps/server/src/codebot/security/scanners/gitleaks.py contains `Severity.CRITICAL`
    - apps/server/src/codebot/security/scanners/allowlist.py contains `class AllowlistValidator:`
    - apps/server/src/codebot/security/scanners/allowlist.py contains `async def validate_requirements(`
    - apps/server/src/codebot/security/scanners/allowlist.py contains `async def validate_package_json(`
    - All 4 test files exit 0
  </acceptance_criteria>
  <done>Three scanner adapters (Semgrep, Trivy, Gitleaks) normalize CLI JSON output into ScanResult. AllowlistValidator checks pip/npm dependencies. All unit tests pass with mocked CLI output.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/v13478/Desktop/CodeBot && uv run pytest apps/server/tests/unit/test_security_gate.py apps/server/tests/unit/test_security_semgrep.py apps/server/tests/unit/test_security_trivy.py apps/server/tests/unit/test_security_gitleaks.py apps/server/tests/unit/test_allowlist.py -x -v
```
All 5 test files pass. Security package importable: `python -c "from codebot.security import SecurityGate, ScanResult"`.
</verification>

<success_criteria>
- SecurityGate correctly evaluates pass/fail based on thresholds
- Three scanner adapters parse their respective CLI JSON output formats
- AllowlistValidator rejects unknown packages for both pip and npm
- All unit tests pass
- Config YAML files exist with sensible defaults
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-pipeline-worktree-manager/08-01-SUMMARY.md`
</output>
