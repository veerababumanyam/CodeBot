---
phase: 09-full-agent-roster
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/server/src/codebot/agents/architect.py
  - apps/server/src/codebot/agents/designer.py
  - apps/server/src/codebot/agents/template_curator.py
  - apps/server/src/codebot/agents/database_designer.py
  - apps/server/src/codebot/agents/api_designer.py
  - apps/server/src/codebot/agents/planner.py
  - apps/server/src/codebot/agents/techstack_builder.py
  - configs/agents/architect.yaml
  - configs/agents/designer.yaml
  - configs/agents/template.yaml
  - configs/agents/database.yaml
  - configs/agents/api_gateway.yaml
  - configs/agents/planner.yaml
  - configs/agents/techstack_builder.yaml
  - tests/unit/agents/test_architecture_agents.py
  - tests/unit/agents/test_planning_agents.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - INPT-06
  - INPT-07

must_haves:
  truths:
    - "Five S3 Architecture agents exist, extend BaseAgent, and can be instantiated from the registry"
    - "S3 agents write to separate SharedState namespaces for parallel safety"
    - "Two S4 Planning agents decompose architecture into implementable tasks"
    - "All seven agents have YAML configs with tools, context tiers, and retry policies"
    - "Template agent supports UI framework selection (Shadcn/ui, Tailwind UI, Material Design)"
    - "TechStack agent can recommend and validate technology choices"
  artifacts:
    - path: "apps/server/src/codebot/agents/architect.py"
      provides: "ArchitectAgent(BaseAgent) for system architecture design"
      contains: "class ArchitectAgent(BaseAgent)"
    - path: "apps/server/src/codebot/agents/designer.py"
      provides: "DesignerAgent(BaseAgent) for UI/UX wireframes and component hierarchy"
      contains: "class DesignerAgent(BaseAgent)"
    - path: "apps/server/src/codebot/agents/database_designer.py"
      provides: "DatabaseDesignerAgent(BaseAgent) for schema and migrations"
      contains: "class DatabaseDesignerAgent(BaseAgent)"
    - path: "apps/server/src/codebot/agents/api_designer.py"
      provides: "APIDesignerAgent(BaseAgent) for REST/GraphQL spec generation"
      contains: "class APIDesignerAgent(BaseAgent)"
    - path: "apps/server/src/codebot/agents/planner.py"
      provides: "PlannerAgent(BaseAgent) for task decomposition with dependencies"
      contains: "class PlannerAgent(BaseAgent)"
  key_links:
    - from: "apps/server/src/codebot/agents/architect.py"
      to: "apps/server/src/codebot/agents/registry.py"
      via: "@register_agent decorator"
      pattern: "@register_agent\\(AgentType\\.ARCHITECT\\)"
    - from: "apps/server/src/codebot/agents/planner.py"
      to: "apps/server/src/codebot/agents/registry.py"
      via: "@register_agent decorator"
      pattern: "@register_agent\\(AgentType\\.PLANNER\\)"
---

<objective>
Implement all S3 Architecture agents (Architect, Designer, Template, Database, API Gateway) and S4 Planning agents (Planner, TechStack Builder).

Purpose: These 7 agents form the architecture and planning stages of the pipeline. S3 agents execute in parallel during pipeline runs (ARCH-05), each writing to its own SharedState namespace. S4 agents decompose architecture into implementation tasks.

Output: 7 agent classes extending BaseAgent with @register_agent decorators, 7 YAML configs, unit tests for all agents.
</objective>

<execution_context>
@/Users/v13478/.claude/get-shit-done/workflows/execute-plan.md
@/Users/v13478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-full-agent-roster/09-RESEARCH.md
@.planning/phases/09-full-agent-roster/09-01-SUMMARY.md
@.claude/skills/codebot-agent-implementation/SKILL.md

<interfaces>
<!-- From Plan 01 -- the registry pattern all agents must follow -->

From apps/server/src/codebot/agents/registry.py (created in Plan 01):
```python
from agent_sdk.models.enums import AgentType

_REGISTRY: dict[AgentType, type] = {}

def register_agent(agent_type: AgentType):
    """Decorator to register an agent class."""
    def decorator(cls):
        _REGISTRY[agent_type] = cls
        return cls
    return decorator

def create_agent(agent_type: AgentType, **kwargs):
    """Factory: instantiate by type."""
    ...
```

From libs/agent-sdk/src/agent_sdk/agents/base.py (created in Plan 01):
```python
from dataclasses import dataclass

@dataclass(slots=True, kw_only=True)
class BaseAgent:
    """Base class all agents must extend."""
    pass
```

From libs/agent-sdk/src/agent_sdk/models/enums.py:
```python
class AgentType(str, enum.Enum):
    ARCHITECT = "ARCHITECT"
    DESIGNER = "DESIGNER"
    TEMPLATE_CURATOR = "TEMPLATE_CURATOR"
    API_DESIGNER = "API_DESIGNER"
    PLANNER = "PLANNER"
    TECH_STACK_ADVISOR = "TECH_STACK_ADVISOR"
```

Agent class pattern (from Phase 9 research + codebot-agent-implementation skill):
```python
from agent_sdk.agents.base import BaseAgent

@register_agent(AgentType.XXX)
@dataclass(slots=True, kw_only=True)
class XxxAgent(BaseAgent):
    agent_type: AgentType = field(default=AgentType.XXX, init=False)
    name: str = "xxx"
    model_tier: str = "tier1"
    tools: list[str] = field(default_factory=lambda: [...])

    async def perceive(self, shared_state: dict[str, Any]) -> dict[str, Any]: ...
    async def reason(self, context: dict[str, Any]) -> dict[str, Any]: ...
    async def act(self, plan: dict[str, Any]) -> dict[str, Any]: ...
    async def review(self, result: dict[str, Any]) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Implement Architect and Designer agents (2 S3 agents)</name>
  <files>
    apps/server/src/codebot/agents/architect.py,
    apps/server/src/codebot/agents/designer.py,
    configs/agents/architect.yaml,
    configs/agents/designer.yaml
  </files>
  <read_first>
    apps/server/src/codebot/agents/registry.py,
    apps/server/src/codebot/agents/brainstorming.py,
    libs/agent-sdk/src/agent_sdk/agents/base.py,
    libs/agent-sdk/src/agent_sdk/models/enums.py,
    tests/unit/agents/conftest.py
  </read_first>
  <action>
    Follow the exact same structural pattern as brainstorming.py. All agents MUST: `from agent_sdk.agents.base import BaseAgent` and use `class XxxAgent(BaseAgent)`. Each agent file must have: `from __future__ import annotations`, logging, `@register_agent`, `@dataclass(slots=True, kw_only=True)`, PRA cycle methods, and a `SYSTEM_PROMPT` constant.

    **1. architect.py** -- `@register_agent(AgentType.ARCHITECT)`, class `ArchitectAgent(BaseAgent)`:
       - `tools: ["diagram_generator", "pattern_library", "adr_writer", "component_designer"]`
       - `perceive`: extracts `research_output`, `project_requirements`, `tech_stack`
       - `act`: returns `{"architecture_doc": ..., "component_diagram": ..., "data_flow": ..., "adr_records": ...}`
       - `review`: validates `architecture_doc` and `component_diagram` exist
       - `state_updates` key: `"architect_output"`
       - SYSTEM_PROMPT: role = system architect, responsibilities include component boundary design (ARCH-01), data flow definition, ADR authoring, pattern selection. Output: architecture document with C4 diagrams.

    **2. designer.py** -- `@register_agent(AgentType.DESIGNER)`, class `DesignerAgent(BaseAgent)`:
       - `tools: ["wireframe_generator", "component_tree_builder", "color_palette", "layout_designer"]`
       - `perceive`: extracts `research_output`, `project_requirements`, `architect_output` (if available)
       - `act`: returns `{"wireframes": ..., "component_hierarchy": ..., "design_tokens": ..., "responsive_specs": ...}`
       - `review`: validates `wireframes` and `component_hierarchy` exist
       - `state_updates` key: `"designer_output"`
       - SYSTEM_PROMPT: role = UI/UX designer (ARCH-04), wireframe generation, component hierarchy creation

    **YAML configs** for both agents in `configs/agents/`:
    - `architect.yaml`: model claude-opus-4, temperature 0.3, timeout 600, tools as listed
    - `designer.yaml`: model claude-opus-4, temperature 0.5, timeout 600, tools as listed
    Both follow the established format from brainstorming.yaml.
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/unit/agents/test_architecture_agents.py -k "architect or designer" -x -v --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/server/src/codebot/agents/architect.py` contains `class ArchitectAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent`
    - File `apps/server/src/codebot/agents/designer.py` contains `class DesignerAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent`
    - Each agent file contains `async def perceive`, `async def reason`, `async def act`, `async def review`
    - Files `configs/agents/architect.yaml` and `configs/agents/designer.yaml` exist
  </acceptance_criteria>
  <done>Architect and Designer agents extend BaseAgent, are registered, and have YAML configs.</done>
</task>

<task type="auto">
  <name>Task 1b: Implement Template, Database, and API Designer agents (3 S3 agents) with architecture test suite</name>
  <files>
    apps/server/src/codebot/agents/template_curator.py,
    apps/server/src/codebot/agents/database_designer.py,
    apps/server/src/codebot/agents/api_designer.py,
    configs/agents/template.yaml,
    configs/agents/database.yaml,
    configs/agents/api_gateway.yaml,
    tests/unit/agents/test_architecture_agents.py
  </files>
  <read_first>
    apps/server/src/codebot/agents/registry.py,
    apps/server/src/codebot/agents/architect.py,
    libs/agent-sdk/src/agent_sdk/agents/base.py,
    libs/agent-sdk/src/agent_sdk/models/enums.py,
    tests/unit/agents/conftest.py
  </read_first>
  <action>
    All agents MUST: `from agent_sdk.agents.base import BaseAgent` and use `class XxxAgent(BaseAgent)`.

    **1. template_curator.py** -- `@register_agent(AgentType.TEMPLATE_CURATOR)`, class `TemplateCuratorAgent(BaseAgent)`:
       - `tools: ["template_registry", "scaffold_generator", "config_renderer"]`
       - `perceive`: extracts `designer_output`, `tech_stack`, `user_preferences` including template selection (INPT-06)
       - `act`: returns `{"selected_template": ..., "template_config": ..., "scaffold_files": ...}`
       - `review`: validates `selected_template` exists
       - `state_updates` key: `"template_output"`
       - SYSTEM_PROMPT: role = template curator, supports Shadcn/ui, Tailwind UI, Material Design, custom templates

    **2. database_designer.py** -- Class `DatabaseDesignerAgent(BaseAgent)` as `@dataclass(slots=True, kw_only=True)` WITHOUT `@register_agent` (no matching AgentType enum value):
       - `agent_type: AgentType = field(default=AgentType.ARCHITECT, init=False)` (uses ARCHITECT as category fallback)
       - `name: str = "database_designer"`
       - `tools: ["schema_generator", "migration_generator", "erd_generator", "sql_validator"]`
       - `act`: returns `{"database_schema": ..., "migrations": ..., "erd_diagram": ..., "indexes": ...}`
       - `state_updates` key: `"database_output"`
       - SYSTEM_PROMPT: role = database designer (ARCH-03), schema design with migrations

    **3. api_designer.py** -- `@register_agent(AgentType.API_DESIGNER)`, class `APIDesignerAgent(BaseAgent)`:
       - `tools: ["openapi_designer", "graphql_designer", "api_validator", "endpoint_generator"]`
       - `act`: returns `{"api_spec": ..., "endpoint_definitions": ..., "auth_scheme": ..., "rate_limiting": ...}`
       - `state_updates` key: `"api_designer_output"`
       - SYSTEM_PROMPT: role = API designer (ARCH-02), generates REST/GraphQL API specifications

    **YAML configs:**
    - `template.yaml`: model claude-sonnet-4, temperature 0.2, timeout 300, tools as listed
    - `database.yaml`: model claude-opus-4, temperature 0.2, timeout 600, tools as listed
    - `api_gateway.yaml`: model claude-opus-4, temperature 0.2, timeout 600, tools as listed
    All follow established format from brainstorming.yaml.

    **tests/unit/agents/test_architecture_agents.py:**
    - For each of the 5 agents: `test_{name}_agent_type`, `test_{name}_extends_base_agent` (assert isinstance(agent, BaseAgent)), `test_{name}_system_prompt_not_empty`, `test_{name}_review_passes_with_valid_output`, `test_{name}_review_fails_with_empty_output`
    - `test_all_s3_agents_write_different_state_keys`: instantiate all 5, call review with valid data, assert each returns a different `state_updates` key (architect_output, designer_output, template_output, database_output, api_designer_output) -- proves parallel safety (ARCH-05)
    - `test_architect_yaml_config_loads`, `test_designer_yaml_config_loads` etc.
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/unit/agents/test_architecture_agents.py -x -v --timeout=30 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/server/src/codebot/agents/template_curator.py` contains `class TemplateCuratorAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent`
    - File `apps/server/src/codebot/agents/api_designer.py` contains `class APIDesignerAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent`
    - File `apps/server/src/codebot/agents/database_designer.py` contains `class DatabaseDesignerAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent`
    - All S3 agents write to distinct SharedState keys (architect_output, designer_output, etc.)
    - All tests in `test_architecture_agents.py` pass
  </acceptance_criteria>
  <done>All five S3 Architecture agents extend BaseAgent with parallel-safe SharedState namespaces. Template agent supports UI framework selection.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 2 S4 Planning agents (Planner, TechStack Builder) with tests</name>
  <files>
    apps/server/src/codebot/agents/planner.py,
    apps/server/src/codebot/agents/techstack_builder.py,
    configs/agents/planner.yaml,
    configs/agents/techstack_builder.yaml,
    tests/unit/agents/test_planning_agents.py
  </files>
  <read_first>
    apps/server/src/codebot/agents/registry.py,
    apps/server/src/codebot/agents/brainstorming.py,
    apps/server/src/codebot/agents/architect.py,
    libs/agent-sdk/src/agent_sdk/agents/base.py,
    libs/agent-sdk/src/agent_sdk/models/enums.py,
    tests/unit/agents/conftest.py
  </read_first>
  <action>
    Follow exact same structural pattern as previous agents. All agents MUST extend BaseAgent.

    **1. planner.py** -- `@register_agent(AgentType.PLANNER)`, class `PlannerAgent(BaseAgent)`:
       - `tools: ["task_decomposer", "dependency_resolver", "complexity_estimator", "parallel_scheduler", "critical_path_analyzer"]`
       - `perceive`: extracts `architect_output`, `designer_output`, `database_output`, `api_designer_output`, `research_output`, `project_requirements`
       - `act`: returns `{"task_graph": ..., "execution_order": ..., "parallel_groups": ..., "complexity_estimates": ..., "acceptance_criteria": ...}`
       - `review`: validates `task_graph` is a non-empty list and each task has `title`, `target_files`, `acceptance_criteria`, `estimated_complexity` keys
       - `state_updates` key: `"planner_output"`
       - SYSTEM_PROMPT: role = implementation planner, responsibilities: task decomposition with dependencies (PLAN-01), execution order and parallelization (PLAN-02), task specs with target files and acceptance criteria (PLAN-03)
       - Output format: JSON array of tasks each with `{id, title, description, target_files: [], acceptance_criteria: [], estimated_complexity: "low"|"medium"|"high", dependencies: [task_ids], parallel_group: str|null}`

    **2. techstack_builder.py** -- `@register_agent(AgentType.TECH_STACK_ADVISOR)`, class `TechStackBuilderAgent(BaseAgent)`:
       - `tools: ["stack_analyzer", "compatibility_checker", "version_resolver", "benchmark_lookup"]`
       - `perceive`: extracts `brainstorming_output`, `research_output`, `user_preferences` (INPT-07 -- user can select or auto-recommend tech stack)
       - `act`: returns `{"recommended_stack": ..., "alternatives": ..., "compatibility_matrix": ..., "version_pins": ...}`
       - `review`: validates `recommended_stack` has keys `language`, `framework`, `database`, `hosting`
       - `state_updates` key: `"techstack_output"`
       - SYSTEM_PROMPT: role = technology stack advisor (INPT-07), recommends and validates technology choices, considers performance, ecosystem maturity, team familiarity

    **YAML configs:**
    - `planner.yaml`: model claude-opus-4, temperature 0.2 (low for structured output), timeout 600
    - `techstack_builder.yaml`: model claude-sonnet-4, temperature 0.3, timeout 300

    **tests/unit/agents/test_planning_agents.py:**
    - For PlannerAgent: `test_agent_type`, `test_extends_base_agent`, `test_perceive_reads_architecture_outputs`, `test_review_passes_with_valid_task_graph`, `test_review_fails_with_empty_task_graph`, `test_review_validates_task_structure` (task must have title, target_files, acceptance_criteria)
    - For TechStackBuilderAgent: `test_agent_type`, `test_extends_base_agent`, `test_perceive_reads_user_preferences`, `test_review_passes_with_valid_stack`, `test_review_fails_without_framework_key`
    - `test_yaml_configs_load` for both
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/unit/agents/test_planning_agents.py -x -v --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/server/src/codebot/agents/planner.py` contains `class PlannerAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent`
    - File `apps/server/src/codebot/agents/techstack_builder.py` contains `class TechStackBuilderAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent`
    - PlannerAgent review validates each task has `title`, `target_files`, `acceptance_criteria`, `estimated_complexity`
    - TechStackBuilderAgent review validates recommended_stack has `language`, `framework`, `database`, `hosting`
    - Files `configs/agents/planner.yaml` and `configs/agents/techstack_builder.yaml` exist
    - All tests in `test_planning_agents.py` pass
  </acceptance_criteria>
  <done>PlannerAgent(BaseAgent) decomposes architecture into implementation tasks (PLAN-01/02/03). TechStackBuilderAgent(BaseAgent) recommends tech stacks (INPT-07). All tests pass.</done>
</task>

</tasks>

<verification>
All test suites pass:
```bash
cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/unit/agents/test_architecture_agents.py tests/unit/agents/test_planning_agents.py -x -v --timeout=30
```
All 7 agents extend BaseAgent, are registered, and are instantiable. S3 agents proved parallel-safe via distinct state keys.
</verification>

<success_criteria>
1. Five S3 Architecture agents extend BaseAgent and follow the registry pattern
2. S3 agents write to separate SharedState namespaces (parallel safety proven by tests)
3. Two S4 Planning agents extend BaseAgent and produce structured task outputs
4. Template agent supports Shadcn/ui, Tailwind UI, Material Design selection (INPT-06)
5. TechStack agent validates language, framework, database, hosting choices (INPT-07)
6. All 7 YAML configs load successfully
7. All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-full-agent-roster/09-02-SUMMARY.md`
</output>
