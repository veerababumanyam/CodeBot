---
phase: 09-full-agent-roster
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - apps/server/src/codebot/agents/doc_writer.py
  - apps/server/src/codebot/agents/devops.py
  - apps/server/src/codebot/agents/github_agent.py
  - apps/server/src/codebot/agents/orchestrator.py
  - apps/server/src/codebot/agents/project_manager.py
  - apps/server/src/codebot/agents/code_reviewer.py
  - apps/server/src/codebot/agents/backend_dev.py
  - apps/server/src/codebot/agents/skill_creator.py
  - apps/server/src/codebot/agents/hooks_creator.py
  - apps/server/src/codebot/agents/tools_creator.py
  - apps/server/src/codebot/agents/__init__.py
  - configs/agents/doc_writer.yaml
  - configs/agents/devops.yaml
  - configs/agents/github.yaml
  - configs/agents/orchestrator.yaml
  - configs/agents/project_manager.yaml
  - configs/agents/code_reviewer.yaml
  - configs/agents/backend_dev.yaml
  - configs/agents/skill_creator.yaml
  - configs/agents/hooks_creator.yaml
  - configs/agents/tools_creator.yaml
  - configs/stages/s3_architecture.yaml
  - configs/stages/s5_implementation.yaml
  - configs/stages/s6_quality.yaml
  - tests/unit/agents/test_doc_writer.py
  - tests/unit/agents/test_remaining_agents.py
  - tests/integration/agents/__init__.py
  - tests/integration/agents/test_agent_registry.py
  - tests/integration/agents/test_event_audit.py
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - EVNT-02
  - EVNT-03
  - EVNT-04

must_haves:
  truths:
    - "Documentation Writer agent generates API docs, user guides, ADRs, and deployment guides"
    - "All 30 agents are registered in the AgentRegistry"
    - "Every agent state transition and tool call can be published as an event for full audit trail"
    - "Stage subgraph YAML configs define fan-out/fan-in topology for S3, S5, S6"
    - "Agent __init__.py imports all agent modules to trigger registration"
  artifacts:
    - path: "apps/server/src/codebot/agents/doc_writer.py"
      provides: "DocumentationWriterAgent for API docs, guides, ADRs"
      contains: "class DocumentationWriterAgent"
    - path: "apps/server/src/codebot/agents/__init__.py"
      provides: "Bootstrap imports for all 30 agent modules"
      min_lines: 30
    - path: "tests/integration/agents/test_agent_registry.py"
      provides: "Integration test verifying all 30 AgentType values have registered classes"
      contains: "test_all_30_agents_registered"
    - path: "tests/integration/agents/test_event_audit.py"
      provides: "Integration test for event sourcing audit trail"
      contains: "test_agent_events_published"
    - path: "configs/stages/s3_architecture.yaml"
      provides: "S3 stage subgraph with parallel fan-out for 5 architecture agents"
      contains: "execution: fan_out_fan_in"
  key_links:
    - from: "apps/server/src/codebot/agents/__init__.py"
      to: "apps/server/src/codebot/agents/registry.py"
      via: "import triggers @register_agent decorators"
      pattern: "from codebot.agents import"
    - from: "tests/integration/agents/test_agent_registry.py"
      to: "apps/server/src/codebot/agents/registry.py"
      via: "get_all_registered() verification"
      pattern: "get_all_registered"
---

<objective>
Implement remaining agents (S9 Documentation, Operations, Cross-cutting, Tooling stubs), complete agent __init__.py bootstrap, create stage subgraph configs, and validate all 30 agents are registered with full event audit trail.

Purpose: This is the capstone plan that completes the full agent roster. It adds the final agents (Documentation, DevOps, GitHub, Orchestrator, Project Manager, Code Reviewer, Backend Dev, and 3 Tooling stubs), wires the __init__.py bootstrap, creates stage subgraph YAML configs for S3/S5/S6 parallel execution, and runs the integration test proving all 30 agents are registered (AGNT-08). Event sourcing for audit trail (EVNT-02/03/04) is verified.

Output: All remaining agent files, complete __init__.py with 30 imports, stage subgraph YAMLs, integration tests for registry and event audit.
</objective>

<execution_context>
@/Users/v13478/.claude/get-shit-done/workflows/execute-plan.md
@/Users/v13478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-full-agent-roster/09-RESEARCH.md
@.planning/phases/09-full-agent-roster/09-01-SUMMARY.md
@.planning/phases/09-full-agent-roster/09-02-SUMMARY.md
@.planning/phases/09-full-agent-roster/09-03-SUMMARY.md
@.planning/phases/09-full-agent-roster/09-04-SUMMARY.md

<interfaces>
From libs/agent-sdk/src/agent_sdk/models/enums.py (ALL 30 AgentType values):
```python
class AgentType(str, enum.Enum):
    ORCHESTRATOR = "ORCHESTRATOR"
    PLANNER = "PLANNER"
    RESEARCHER = "RESEARCHER"
    ARCHITECT = "ARCHITECT"
    DESIGNER = "DESIGNER"
    FRONTEND_DEV = "FRONTEND_DEV"
    BACKEND_DEV = "BACKEND_DEV"
    MIDDLEWARE_DEV = "MIDDLEWARE_DEV"
    INFRA_ENGINEER = "INFRA_ENGINEER"
    SECURITY_AUDITOR = "SECURITY_AUDITOR"
    CODE_REVIEWER = "CODE_REVIEWER"
    TESTER = "TESTER"
    DEBUGGER = "DEBUGGER"
    DOC_WRITER = "DOC_WRITER"
    BRAINSTORM_FACILITATOR = "BRAINSTORM_FACILITATOR"
    TECH_STACK_ADVISOR = "TECH_STACK_ADVISOR"
    TEMPLATE_CURATOR = "TEMPLATE_CURATOR"
    DEPLOYER = "DEPLOYER"
    COLLABORATION_MANAGER = "COLLABORATION_MANAGER"
    MOBILE_DEV = "MOBILE_DEV"
    PERFORMANCE_TESTER = "PERFORMANCE_TESTER"
    ACCESSIBILITY_AUDITOR = "ACCESSIBILITY_AUDITOR"
    GITHUB_INTEGRATOR = "GITHUB_INTEGRATOR"
    SKILL_MANAGER = "SKILL_MANAGER"
    HOOK_MANAGER = "HOOK_MANAGER"
    TOOL_BUILDER = "TOOL_BUILDER"
    INTEGRATION_ADAPTER = "INTEGRATION_ADAPTER"
    I18N_SPECIALIST = "I18N_SPECIALIST"
    API_DESIGNER = "API_DESIGNER"
    PROJECT_MANAGER = "PROJECT_MANAGER"
```

From libs/agent-sdk/src/agent_sdk/models/events.py:
```python
class EventEnvelope(BaseModel):
    event_type: EventType
    source_agent_id: uuid.UUID | None = None
    payload: bytes
    timestamp: datetime
```

From apps/server/src/codebot/events/bus.py:
```python
async def publish_event(bus: EventBus, envelope: EventEnvelope) -> None:
    event_type_slug = envelope.event_type.value.lower().replace("_", ".")
    payload = envelope.model_dump_json().encode()
    await bus.publish(event_type_slug, payload)
```

Agents already created (Plans 01-04):
- Plan 01: registry.py, brainstorming.py, researcher.py
- Plan 02: architect.py, designer.py, template_curator.py, database_designer.py, api_designer.py, planner.py, techstack_builder.py
- Plan 03: frontend_dev.py, mobile_dev.py, infra_engineer.py, middleware_dev.py, integrations.py
- Plan 04: security_auditor.py, accessibility.py, performance.py, i18n_l10n.py, tester.py, debugger.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement remaining 10 agents (Docs, Ops, Cross-cutting, Tooling stubs)</name>
  <files>
    apps/server/src/codebot/agents/doc_writer.py,
    apps/server/src/codebot/agents/devops.py,
    apps/server/src/codebot/agents/github_agent.py,
    apps/server/src/codebot/agents/orchestrator.py,
    apps/server/src/codebot/agents/project_manager.py,
    apps/server/src/codebot/agents/code_reviewer.py,
    apps/server/src/codebot/agents/backend_dev.py,
    apps/server/src/codebot/agents/skill_creator.py,
    apps/server/src/codebot/agents/hooks_creator.py,
    apps/server/src/codebot/agents/tools_creator.py,
    configs/agents/doc_writer.yaml,
    configs/agents/devops.yaml,
    configs/agents/github.yaml,
    configs/agents/orchestrator.yaml,
    configs/agents/project_manager.yaml,
    configs/agents/code_reviewer.yaml,
    configs/agents/backend_dev.yaml,
    configs/agents/skill_creator.yaml,
    configs/agents/hooks_creator.yaml,
    configs/agents/tools_creator.yaml,
    tests/unit/agents/test_doc_writer.py,
    tests/unit/agents/test_remaining_agents.py
  </files>
  <read_first>
    apps/server/src/codebot/agents/registry.py,
    apps/server/src/codebot/agents/brainstorming.py,
    libs/agent-sdk/src/agent_sdk/models/enums.py,
    tests/unit/agents/conftest.py
  </read_first>
  <action>
    Follow the exact same structural pattern as brainstorming.py for all agents. Each agent: `from __future__ import annotations`, `@register_agent(AgentType.XXX)`, `@dataclass(slots=True, kw_only=True)`, PRA cycle methods, `SYSTEM_PROMPT` constant.

    **S9 Documentation agents (3 agents):**

    **1. doc_writer.py** -- `@register_agent(AgentType.DOC_WRITER)`, class `DocumentationWriterAgent`:
       - `tools: ["file_read", "file_write", "openapi_renderer", "docstring_generator", "readme_generator", "diagram_renderer", "adr_formatter", "deployment_guide_generator"]`
       - `perceive`: extracts all `*_output` keys from shared_state, `architect_output` (for ADRs), `api_designer_output` (for API docs), `infra_engineer_output` (for deployment guides)
       - `act`: returns `{"api_docs": ..., "user_guide": ..., "setup_instructions": ..., "adr_records": [...], "deployment_guide": ..., "generated_files": [...]}`
       - `review`: validates `api_docs` and `user_guide` exist
       - `state_updates` key: `"doc_writer_output"`
       - SYSTEM_PROMPT: role = documentation writer. DOCS-01: generates API docs from code (OpenAPI rendering). DOCS-02: creates user guides and setup instructions. DOCS-03: produces ADRs. DOCS-04: includes deployment guides from infra config.

    **2. devops.py** -- `@register_agent(AgentType.DEPLOYER)`, class `DevOpsAgent`:
       - `tools: ["file_read", "file_write", "bash", "docker_build", "helm_template", "ci_cd_validator"]`
       - `perceive`: extracts `infra_engineer_output`, `architect_output`
       - `act`: returns `{"deployment_configs": [...], "ci_cd_pipelines": [...], "monitoring_config": ..., "generated_files": [...]}`
       - `review`: validates `deployment_configs` is non-empty
       - `state_updates` key: `"devops_output"`
       - SYSTEM_PROMPT: role = DevOps engineer, manages deployment configurations, CI/CD pipeline generation, monitoring setup

    **3. github_agent.py** -- `@register_agent(AgentType.GITHUB_INTEGRATOR)`, class `GitHubAgent`:
       - `tools: ["github_api", "pr_creator", "issue_tracker", "webhook_manager", "release_manager"]`
       - `perceive`: extracts `tester_output`, `debugger_output`, project config for repo URL
       - `act`: returns `{"pull_requests": [...], "issues": [...], "releases": [...], "webhook_configs": [...]}`
       - `review`: validates output is well-formed
       - `state_updates` key: `"github_output"`
       - SYSTEM_PROMPT: role = GitHub integration specialist, creates PRs, manages issues, handles webhook events, prepares releases

    **Cross-cutting agents (3 agents):**

    **4. orchestrator.py** -- `@register_agent(AgentType.ORCHESTRATOR)`, class `OrchestratorAgent`:
       - `tools: ["pipeline_controller", "agent_monitor", "task_router", "checkpoint_manager", "multimodal_input_processor"]`
       - `perceive`: extracts `user_input` (supporting text, images, URLs per INPT-03), `project_config`, pipeline state
       - `act`: returns `{"pipeline_plan": ..., "stage_configs": [...], "agent_assignments": [...], "input_processed": ...}`
       - `review`: validates `pipeline_plan` exists
       - `state_updates` key: `"orchestrator_output"`
       - SYSTEM_PROMPT: role = pipeline orchestrator, manages full SDLC pipeline execution, routes tasks to agents, handles multi-modal input (INPT-03: text, images, URLs), imports existing codebases (INPT-08)

    **5. project_manager.py** -- `@register_agent(AgentType.PROJECT_MANAGER)`, class `ProjectManagerAgent`:
       - `tools: ["status_reporter", "timeline_tracker", "risk_monitor", "resource_allocator"]`
       - `perceive`: extracts all `*_output` keys, pipeline metrics
       - `act`: returns `{"status_report": ..., "timeline": ..., "risks": [...], "recommendations": [...]}`
       - `review`: validates `status_report` exists
       - `state_updates` key: `"project_manager_output"`
       - SYSTEM_PROMPT: role = project manager, tracks pipeline progress, reports status, identifies risks, recommends resource allocation

    **6. code_reviewer.py** -- `@register_agent(AgentType.CODE_REVIEWER)`, class `CodeReviewerAgent`:
       - `tools: ["file_read", "code_analyzer", "pattern_detector", "complexity_analyzer", "review_comment_writer"]`
       - `perceive`: extracts all `*_dev_output` keys (code to review), `architect_output` (architecture decisions to validate against)
       - `act`: returns `{"review_comments": [...], "approval_status": "approved"|"changes_requested"|"rejected", "code_quality_score": float, "pattern_violations": [...]}`
       - `review`: validates `review_comments` is a list and `approval_status` is present
       - `state_updates` key: `"code_reviewer_output"`
       - SYSTEM_PROMPT: role = code reviewer, reviews generated code for correctness, patterns, maintainability, performance, follows AGENT_CATALOG code review spec

    **7. backend_dev.py** -- `@register_agent(AgentType.BACKEND_DEV)`, class `BackendDevAgent`:
       - `tools: ["file_read", "file_write", "file_edit", "bash", "api_generator", "db_query_builder", "test_runner"]`
       - `use_worktree: bool = True`
       - `perceive`: extracts `planner_output`, `architect_output`, `api_designer_output`, `database_output`, `techstack_output`
       - `act`: returns `{"generated_files": [...], "api_endpoints": [...], "db_models": [...], "test_stubs": [...]}`
       - `review`: validates `generated_files` is non-empty
       - `state_updates` key: `"backend_dev_output"`
       - SYSTEM_PROMPT: role = backend developer, generates Python/FastAPI server code from API specs, follows project conventions (Pydantic v2, SQLAlchemy 2.0, async-first)

    **Tooling agent stubs (3 agents -- minimal implementations, full functionality in Phase 11):**

    **8. skill_creator.py** -- `@register_agent(AgentType.SKILL_MANAGER)`, class `SkillCreatorAgent`:
       - `tools: ["file_read", "file_write"]`
       - PRA methods return placeholder outputs: `act` returns `{"message": "Skill creation deferred to Phase 11", "stub": True}`
       - `state_updates` key: `"skill_creator_output"`
       - SYSTEM_PROMPT: "You are the Skill Creator agent. Full functionality will be implemented in Phase 11."

    **9. hooks_creator.py** -- `@register_agent(AgentType.HOOK_MANAGER)`, class `HooksCreatorAgent`:
       - Same stub pattern as skill_creator
       - `state_updates` key: `"hooks_creator_output"`

    **10. tools_creator.py** -- `@register_agent(AgentType.TOOL_BUILDER)`, class `ToolsCreatorAgent`:
       - Same stub pattern as skill_creator
       - `state_updates` key: `"tools_creator_output"`

    **YAML configs for all 10 agents** (follow established format with agent_type, model, fallback_model, provider, max_tokens, temperature, tools, context_tiers, retry_policy, timeout, settings):
    - `doc_writer.yaml`: agent_type DOC_WRITER, model claude-sonnet-4, temperature 0.3, timeout 600
    - `devops.yaml`: agent_type DEPLOYER, model claude-sonnet-4, temperature 0.2, timeout 600
    - `github.yaml`: agent_type GITHUB_INTEGRATOR, model claude-sonnet-4, temperature 0.2, timeout 300
    - `orchestrator.yaml`: agent_type ORCHESTRATOR, model claude-opus-4, temperature 0.3, timeout 1200
    - `project_manager.yaml`: agent_type PROJECT_MANAGER, model claude-sonnet-4, temperature 0.3, timeout 300
    - `code_reviewer.yaml`: agent_type CODE_REVIEWER, model claude-opus-4, temperature 0.2, timeout 600
    - `backend_dev.yaml`: agent_type BACKEND_DEV, model claude-sonnet-4, temperature 0.2, timeout 900, `use_worktree: true`
    - `skill_creator.yaml`: agent_type SKILL_MANAGER, model claude-sonnet-4, temperature 0.3, timeout 300
    - `hooks_creator.yaml`: agent_type HOOK_MANAGER, model claude-sonnet-4, temperature 0.3, timeout 300
    - `tools_creator.yaml`: agent_type TOOL_BUILDER, model claude-sonnet-4, temperature 0.3, timeout 300

    **tests/unit/agents/test_doc_writer.py:**
    - `test_agent_type_is_doc_writer`
    - `test_system_prompt_mentions_api_docs`: assert "API" in SYSTEM_PROMPT
    - `test_system_prompt_mentions_adr`: assert "ADR" in SYSTEM_PROMPT or "architecture decision" in SYSTEM_PROMPT.lower()
    - `test_review_passes_with_api_docs_and_guide`: pass valid data, assert `review_passed is True`
    - `test_review_fails_without_api_docs`: pass `{"user_guide": "..."}`, assert `review_passed is False`
    - `test_yaml_config_loads`

    **tests/unit/agents/test_remaining_agents.py:**
    - For each of the remaining 9 agents (DevOps, GitHub, Orchestrator, ProjectManager, CodeReviewer, BackendDev, SkillCreator, HooksCreator, ToolsCreator):
      - `test_{name}_agent_type`: correct AgentType value
      - `test_{name}_registered`: import the module, verify `get_all_registered()` includes the type
    - `test_tooling_stubs_return_stub_flag`: for SkillCreator, HooksCreator, ToolsCreator, call `act`, verify `result.get("stub") is True`
    - `test_yaml_configs_load_all_10`: load all 10 YAML config files from configs/agents/
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/unit/agents/test_doc_writer.py tests/unit/agents/test_remaining_agents.py -x -v --timeout=30 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/server/src/codebot/agents/doc_writer.py` contains `@register_agent(AgentType.DOC_WRITER)` and `class DocumentationWriterAgent`
    - File `apps/server/src/codebot/agents/orchestrator.py` contains `@register_agent(AgentType.ORCHESTRATOR)` and `"multimodal_input_processor"` in tools (INPT-03)
    - File `apps/server/src/codebot/agents/code_reviewer.py` contains `@register_agent(AgentType.CODE_REVIEWER)`
    - File `apps/server/src/codebot/agents/backend_dev.py` contains `@register_agent(AgentType.BACKEND_DEV)` and `use_worktree: bool = True`
    - Files `skill_creator.py`, `hooks_creator.py`, `tools_creator.py` each contain `"stub": True` in act return
    - All 10 YAML config files exist in `configs/agents/`
    - All tests pass
  </acceptance_criteria>
  <done>All remaining agents implemented. Documentation writer covers DOCS-01 through DOCS-04. Tooling agents are registered stubs. All YAML configs exist.</done>
</task>

<task type="auto">
  <name>Task 2: Complete __init__.py bootstrap, stage subgraph configs, and integration tests for full registry and event audit</name>
  <files>
    apps/server/src/codebot/agents/__init__.py,
    configs/stages/s3_architecture.yaml,
    configs/stages/s5_implementation.yaml,
    configs/stages/s6_quality.yaml,
    tests/integration/agents/__init__.py,
    tests/integration/agents/test_agent_registry.py,
    tests/integration/agents/test_event_audit.py
  </files>
  <read_first>
    apps/server/src/codebot/agents/__init__.py,
    apps/server/src/codebot/agents/registry.py,
    libs/agent-sdk/src/agent_sdk/models/enums.py,
    libs/agent-sdk/src/agent_sdk/models/events.py,
    apps/server/src/codebot/events/bus.py
  </read_first>
  <action>
    **1. Update `apps/server/src/codebot/agents/__init__.py`** to import all 30 agent modules, triggering `@register_agent` decorators:
       ```python
       """Agent package -- imports trigger registration via @register_agent decorators."""
       from codebot.agents.registry import create_agent, get_all_registered, register_agent  # noqa: F401

       # Import all agent modules to trigger @register_agent decorators.
       # Each module's @register_agent decorator adds the class to _REGISTRY.
       from codebot.agents import (  # noqa: F401
           brainstorming,
           researcher,
           architect,
           designer,
           template_curator,
           database_designer,
           api_designer,
           planner,
           techstack_builder,
           frontend_dev,
           backend_dev,
           middleware_dev,
           mobile_dev,
           infra_engineer,
           integrations,
           code_reviewer,
           security_auditor,
           accessibility,
           performance,
           i18n_l10n,
           tester,
           debugger,
           doc_writer,
           devops,
           github_agent,
           orchestrator,
           project_manager,
           skill_creator,
           hooks_creator,
           tools_creator,
       )
       ```
       NOTE: `database_designer` is not registered via decorator (no enum value), so it does NOT add to _REGISTRY. The final count of registered agents should be 29 from decorators. The DatabaseDesignerAgent is available as a direct import. However, if you need exactly 30 registered, consider mapping database_designer to a sub-key or accepting 29 + 1 direct-import. Per AGENT_CATALOG there are 30 agents but the enum has 30 values -- we need to check: the enum has ORCHESTRATOR, PLANNER, RESEARCHER, ARCHITECT, DESIGNER, FRONTEND_DEV, BACKEND_DEV, MIDDLEWARE_DEV, INFRA_ENGINEER, SECURITY_AUDITOR, CODE_REVIEWER, TESTER, DEBUGGER, DOC_WRITER, BRAINSTORM_FACILITATOR, TECH_STACK_ADVISOR, TEMPLATE_CURATOR, DEPLOYER, COLLABORATION_MANAGER, MOBILE_DEV, PERFORMANCE_TESTER, ACCESSIBILITY_AUDITOR, GITHUB_INTEGRATOR, SKILL_MANAGER, HOOK_MANAGER, TOOL_BUILDER, INTEGRATION_ADAPTER, I18N_SPECIALIST, API_DESIGNER, PROJECT_MANAGER = 30 values.

       Current assignments: BrainstormFacilitator, Researcher, Architect, Designer, TemplateCurator, APIDesigner, Planner, TechStackAdvisor, FrontendDev, BackendDev, MiddlewareDev, MobileDev, InfraEngineer, IntegrationAdapter, SecurityAuditor, AccessibilityAuditor, PerformanceTester, I18nSpecialist, CodeReviewer, Tester, Debugger, DocWriter, Deployer, GithubIntegrator, Orchestrator, ProjectManager, SkillManager, HookManager, ToolBuilder = 29.

       Missing: COLLABORATION_MANAGER. We need a stub for it.

       Add `apps/server/src/codebot/agents/collaboration_manager.py`: `@register_agent(AgentType.COLLABORATION_MANAGER)`, class `CollaborationManagerAgent` stub (same pattern as tooling stubs, returns `{"stub": True}`). Add YAML config `configs/agents/collaboration_manager.yaml`. Add to __init__.py imports.

       Also: DatabaseDesignerAgent currently has no matching enum. Re-map database_designer to NOT use @register_agent (keep as-is from Plan 02). This means 30 registered agents total (29 from plans + 1 CollaborationManager).

    **2. Create stage subgraph YAML configs:**

    `configs/stages/s3_architecture.yaml`:
    ```yaml
    stage:
      name: architecture_and_design
      pipeline_stage: S3
      type: subgraph
      execution: fan_out_fan_in
      entry_gate:
        type: automatic
        check: research_output_completeness
      agents:
        - id: architect
          type: ARCHITECT
          parallel_group: design
        - id: designer
          type: DESIGNER
          parallel_group: design
        - id: template_curator
          type: TEMPLATE_CURATOR
          parallel_group: design
        - id: api_designer
          type: API_DESIGNER
          parallel_group: design
      merge_strategy:
        type: state_merge
        conflict_resolution: architect_authority
      exit_gate:
        type: approval
        gate_id: G3
        timeout_minutes: 30
        auto_approve_on_timeout: false
    ```

    `configs/stages/s5_implementation.yaml`:
    ```yaml
    stage:
      name: implementation
      pipeline_stage: S5
      type: subgraph
      execution: fan_out_fan_in
      entry_gate:
        type: automatic
        check: planning_output_completeness
      agents:
        - id: frontend_dev
          type: FRONTEND_DEV
          parallel_group: implementation
          use_worktree: true
        - id: backend_dev
          type: BACKEND_DEV
          parallel_group: implementation
          use_worktree: true
        - id: middleware_dev
          type: MIDDLEWARE_DEV
          parallel_group: implementation
          use_worktree: true
        - id: mobile_dev
          type: MOBILE_DEV
          parallel_group: implementation
          use_worktree: true
        - id: infra_engineer
          type: INFRA_ENGINEER
          parallel_group: implementation
          use_worktree: true
      merge_strategy:
        type: worktree_merge
        conflict_resolution: sequential
      exit_gate:
        type: automatic
        check: all_agents_completed
    ```

    `configs/stages/s6_quality.yaml`:
    ```yaml
    stage:
      name: quality_assurance
      pipeline_stage: S6
      type: subgraph
      execution: fan_out_fan_in
      entry_gate:
        type: automatic
        check: implementation_output_completeness
      agents:
        - id: code_reviewer
          type: CODE_REVIEWER
          parallel_group: quality
        - id: security_auditor
          type: SECURITY_AUDITOR
          parallel_group: quality
        - id: accessibility
          type: ACCESSIBILITY_AUDITOR
          parallel_group: quality
        - id: performance
          type: PERFORMANCE_TESTER
          parallel_group: quality
        - id: i18n
          type: I18N_SPECIALIST
          parallel_group: quality
      merge_strategy:
        type: state_merge
        conflict_resolution: gate_aggregation
      exit_gate:
        type: quality_gate
        gate_id: G6
        blocking_severity: ["critical", "high"]
    ```

    **3. Create `tests/integration/agents/__init__.py`** as empty file.

    **4. Create `tests/integration/agents/test_agent_registry.py`:**
    ```python
    """Integration test: verify all 30 AgentType values have registered agent classes."""
    import codebot.agents  # triggers all registrations
    from agent_sdk.models.enums import AgentType
    from codebot.agents.registry import get_all_registered

    class TestAgentRegistryComplete:
        def test_all_30_agents_registered(self):
            """AGNT-08: System supports 30 specialized agents."""
            registered = get_all_registered()
            all_types = set(AgentType)
            registered_types = set(registered.keys())
            missing = all_types - registered_types
            assert len(registered) == 30, f"Expected 30 registered agents, got {len(registered)}. Missing: {missing}"

        def test_each_agent_type_has_class(self):
            """Every AgentType enum value maps to a concrete class."""
            registered = get_all_registered()
            for agent_type in AgentType:
                assert agent_type in registered, f"AgentType.{agent_type.name} has no registered class"

        def test_all_classes_are_unique(self):
            """No two AgentType values map to the same class."""
            registered = get_all_registered()
            classes = list(registered.values())
            assert len(set(id(c) for c in classes)) == len(classes), "Duplicate class registrations found"
    ```

    **5. Create `tests/integration/agents/test_event_audit.py`:**
    ```python
    """Integration test: verify agent events can be published for audit trail."""
    import uuid
    from unittest.mock import AsyncMock, patch

    import pytest

    from agent_sdk.models.enums import AgentStatus, AgentType, EventType
    from agent_sdk.models.events import AgentEvent, EventEnvelope

    class TestEventAudit:
        def test_agent_event_serializes_to_json(self):
            """EVNT-03: Agent events serialize for persistence."""
            event = AgentEvent(
                agent_id=uuid.uuid4(),
                agent_type=AgentType.BRAINSTORM_FACILITATOR,
                status=AgentStatus.RUNNING,
            )
            json_bytes = event.model_dump_json().encode()
            assert b"BRAINSTORM_FACILITATOR" in json_bytes
            assert b"RUNNING" in json_bytes

        def test_event_envelope_wraps_agent_event(self):
            """EVNT-04: EventEnvelope wraps events for reconstruction."""
            agent_event = AgentEvent(
                agent_id=uuid.uuid4(),
                agent_type=AgentType.RESEARCHER,
                status=AgentStatus.COMPLETED,
            )
            envelope = EventEnvelope(
                event_type=EventType.AGENT_COMPLETED,
                source_agent_id=agent_event.agent_id,
                payload=agent_event.model_dump_json().encode(),
            )
            assert envelope.event_type == EventType.AGENT_COMPLETED
            assert envelope.payload is not None
            # Verify payload can be deserialized back (EVNT-02 replay)
            reconstructed = AgentEvent.model_validate_json(envelope.payload)
            assert reconstructed.agent_type == AgentType.RESEARCHER

        @pytest.mark.asyncio
        async def test_publish_event_calls_bus(self):
            """EVNT-03: Events are published via EventBus."""
            from codebot.events.bus import publish_event

            mock_bus = AsyncMock()
            agent_event = AgentEvent(
                agent_id=uuid.uuid4(),
                agent_type=AgentType.TESTER,
                status=AgentStatus.RUNNING,
            )
            envelope = EventEnvelope(
                event_type=EventType.AGENT_STARTED,
                source_agent_id=agent_event.agent_id,
                payload=agent_event.model_dump_json().encode(),
            )
            await publish_event(mock_bus, envelope)
            mock_bus.publish.assert_awaited_once()
            call_args = mock_bus.publish.call_args
            assert "agent.started" in call_args[0][0]  # subject contains event type
    ```
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/integration/agents/test_agent_registry.py tests/integration/agents/test_event_audit.py -x -v --timeout=30 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/server/src/codebot/agents/__init__.py` imports all 30 agent modules (contains `from codebot.agents import` with brainstorming, researcher, architect, designer, etc.)
    - File `tests/integration/agents/test_agent_registry.py` contains `test_all_30_agents_registered` and the test passes (AGNT-08)
    - File `tests/integration/agents/test_event_audit.py` contains `test_agent_event_serializes_to_json` and `test_event_envelope_wraps_agent_event` and `test_publish_event_calls_bus`
    - File `configs/stages/s3_architecture.yaml` contains `execution: fan_out_fan_in` and lists 4 architecture agents
    - File `configs/stages/s5_implementation.yaml` contains `use_worktree: true` for all 5 implementation agents
    - File `configs/stages/s6_quality.yaml` contains `gate_id: G6` and lists 5 QA agents
    - `get_all_registered()` returns exactly 30 entries
    - All integration tests pass
  </acceptance_criteria>
  <done>All 30 agents registered (AGNT-08). Event audit trail verified (EVNT-02/03/04). Stage subgraph YAMLs define parallel execution topology for S3/S5/S6. Full agent roster complete.</done>
</task>

</tasks>

<verification>
Full test suite:
```bash
cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/unit/agents/ tests/integration/agents/ -x -v --timeout=60
```
All 30 agents registered. Event serialization and publishing verified. Stage subgraphs configured.
</verification>

<success_criteria>
1. All 30 AgentType enum values have a registered agent class (AGNT-08)
2. Documentation Writer generates API docs (DOCS-01), user guides (DOCS-02), ADRs (DOCS-03), deployment guides (DOCS-04)
3. Event audit trail works: events serialize, wrap in envelopes, publish to EventBus (EVNT-02/03/04)
4. Stage subgraph YAML configs define parallel topology for S3 (5 agents), S5 (5 agents), S6 (5 agents)
5. __init__.py imports all 30 agent modules triggering registration
6. All unit and integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-full-agent-roster/09-05-SUMMARY.md`
</output>
