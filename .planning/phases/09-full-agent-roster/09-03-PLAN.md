---
phase: 09-full-agent-roster
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/server/src/codebot/agents/frontend_dev.py
  - apps/server/src/codebot/agents/mobile_dev.py
  - apps/server/src/codebot/agents/infra_engineer.py
  - apps/server/src/codebot/agents/middleware_dev.py
  - apps/server/src/codebot/agents/integrations.py
  - configs/agents/frontend_dev.yaml
  - configs/agents/mobile_dev.yaml
  - configs/agents/infra_engineer.yaml
  - configs/agents/middleware_dev.yaml
  - configs/agents/integrations.yaml
  - tests/unit/agents/test_implementation_agents.py
autonomous: true
requirements:
  - IMPL-01
  - IMPL-03
  - IMPL-04

must_haves:
  truths:
    - "Frontend Dev agent extends BaseAgent and generates React/TypeScript code from design specs"
    - "Mobile Dev agent extends BaseAgent and generates cross-platform mobile code"
    - "Infrastructure agent extends BaseAgent and generates Docker, CI/CD, and config files"
    - "All S5 agents write to separate SharedState namespaces for parallel worktree safety"
    - "All S5 agents have YAML configs specifying worktree isolation"
  artifacts:
    - path: "apps/server/src/codebot/agents/frontend_dev.py"
      provides: "FrontendDevAgent(BaseAgent) for React/TypeScript UI code generation"
      contains: "class FrontendDevAgent(BaseAgent)"
    - path: "apps/server/src/codebot/agents/mobile_dev.py"
      provides: "MobileDevAgent(BaseAgent) for cross-platform mobile code"
      contains: "class MobileDevAgent(BaseAgent)"
    - path: "apps/server/src/codebot/agents/infra_engineer.py"
      provides: "InfraEngineerAgent(BaseAgent) for Docker/CI-CD/config generation"
      contains: "class InfraEngineerAgent(BaseAgent)"
  key_links:
    - from: "apps/server/src/codebot/agents/frontend_dev.py"
      to: "apps/server/src/codebot/agents/registry.py"
      via: "@register_agent decorator"
      pattern: "@register_agent\\(AgentType\\.FRONTEND_DEV\\)"
    - from: "apps/server/src/codebot/agents/infra_engineer.py"
      to: "apps/server/src/codebot/agents/registry.py"
      via: "@register_agent decorator"
      pattern: "@register_agent\\(AgentType\\.INFRA_ENGINEER\\)"
    - from: "apps/server/src/codebot/agents/frontend_dev.py"
      to: "libs/agent-sdk/src/agent_sdk/agents/base.py"
      via: "class inheritance"
      pattern: "class FrontendDevAgent\\(BaseAgent\\)"
---

<objective>
Implement all S5 Implementation stage agents: Frontend Dev, Mobile Dev, Infrastructure Engineer, Middleware Dev, and Integrations agent.

Purpose: S5 agents execute in parallel in isolated git worktrees to produce code for their respective platforms. These agents consume architecture and planning outputs from S3/S4 and produce code artifacts that flow to S6 QA.

Output: 5 agent classes extending BaseAgent with YAML configs, unit tests. Each agent uses separate SharedState namespaces for parallel safety.
</objective>

<execution_context>
@/Users/v13478/.claude/get-shit-done/workflows/execute-plan.md
@/Users/v13478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-full-agent-roster/09-RESEARCH.md
@.planning/phases/09-full-agent-roster/09-01-SUMMARY.md
@.claude/skills/codebot-agent-implementation/SKILL.md

<interfaces>
From libs/agent-sdk/src/agent_sdk/agents/base.py:
```python
@dataclass(slots=True, kw_only=True)
class BaseAgent:
    """Base class all agents must extend."""
    pass
```

From libs/agent-sdk/src/agent_sdk/models/enums.py:
```python
class AgentType(str, enum.Enum):
    FRONTEND_DEV = "FRONTEND_DEV"
    BACKEND_DEV = "BACKEND_DEV"
    MIDDLEWARE_DEV = "MIDDLEWARE_DEV"
    MOBILE_DEV = "MOBILE_DEV"
    INFRA_ENGINEER = "INFRA_ENGINEER"
    INTEGRATION_ADAPTER = "INTEGRATION_ADAPTER"
```

Agent class pattern (established in Plan 01 + codebot-agent-implementation skill):
```python
from agent_sdk.agents.base import BaseAgent

@register_agent(AgentType.XXX)
@dataclass(slots=True, kw_only=True)
class XxxAgent(BaseAgent):
    agent_type: AgentType = field(default=AgentType.XXX, init=False)
    name: str = "xxx"
    model_tier: str = "tier1"
    tools: list[str] = field(default_factory=lambda: [...])
    use_worktree: bool = True  # S5 agents need worktree isolation

    async def perceive(self, shared_state: dict[str, Any]) -> dict[str, Any]: ...
    async def reason(self, context: dict[str, Any]) -> dict[str, Any]: ...
    async def act(self, plan: dict[str, Any]) -> dict[str, Any]: ...
    async def review(self, result: dict[str, Any]) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Frontend Dev, Mobile Dev, and Infrastructure Engineer agents</name>
  <files>
    apps/server/src/codebot/agents/frontend_dev.py,
    apps/server/src/codebot/agents/mobile_dev.py,
    apps/server/src/codebot/agents/infra_engineer.py,
    configs/agents/frontend_dev.yaml,
    configs/agents/mobile_dev.yaml,
    configs/agents/infra_engineer.yaml,
    tests/unit/agents/test_implementation_agents.py
  </files>
  <read_first>
    apps/server/src/codebot/agents/registry.py,
    apps/server/src/codebot/agents/brainstorming.py,
    libs/agent-sdk/src/agent_sdk/agents/base.py,
    libs/agent-sdk/src/agent_sdk/models/enums.py,
    tests/unit/agents/conftest.py
  </read_first>
  <action>
    Follow the exact same structural pattern as brainstorming.py. All S5 agents MUST: `from agent_sdk.agents.base import BaseAgent` and use `class XxxAgent(BaseAgent)`. All S5 agents add `use_worktree: bool = True` field for worktree isolation signaling.

    **1. frontend_dev.py** -- `@register_agent(AgentType.FRONTEND_DEV)`, class `FrontendDevAgent(BaseAgent)`:
       - `tools: ["file_read", "file_write", "file_edit", "bash", "browser_preview", "component_generator", "style_generator"]`
       - `use_worktree: bool = True`
       - `perceive`: extracts `planner_output` (task graph), `designer_output` (wireframes/component hierarchy), `template_output` (scaffold), `techstack_output`
       - `act`: returns `{"generated_files": [...], "component_tree": ..., "route_definitions": ..., "test_stubs": [...]}`
       - `review`: validates `generated_files` is non-empty list, each entry has `path` and `content` keys
       - `state_updates` key: `"frontend_dev_output"`
       - SYSTEM_PROMPT: role = frontend developer (IMPL-01), generates React/TypeScript UI code from design specs, follows project conventions (ESM, Tailwind, Zustand, TanStack Query), creates components, routes, state management, and test stubs

    **2. mobile_dev.py** -- `@register_agent(AgentType.MOBILE_DEV)`, class `MobileDevAgent(BaseAgent)`:
       - `tools: ["file_read", "file_write", "file_edit", "bash", "simulator_preview", "cocoapods", "gradle"]`
       - `use_worktree: bool = True`
       - `perceive`: extracts `planner_output`, `designer_output`, `api_designer_output`, `techstack_output`
       - `act`: returns `{"generated_files": [...], "platform_configs": ..., "navigation_tree": ..., "api_bindings": [...]}`
       - `review`: validates `generated_files` is non-empty list
       - `state_updates` key: `"mobile_dev_output"`
       - SYSTEM_PROMPT: role = mobile developer (IMPL-03), generates cross-platform or native mobile code, supports React Native, Flutter, or Swift/Kotlin based on techstack selection

    **3. infra_engineer.py** -- `@register_agent(AgentType.INFRA_ENGINEER)`, class `InfraEngineerAgent(BaseAgent)`:
       - `tools: ["file_read", "file_write", "bash", "terraform_validate", "docker_build", "kubectl", "config_renderer"]`
       - `use_worktree: bool = True`
       - `perceive`: extracts `planner_output`, `architect_output`, `techstack_output`
       - `act`: returns `{"generated_files": [...], "docker_configs": ..., "ci_cd_pipeline": ..., "env_configs": ...}`
       - `review`: validates `generated_files` is non-empty and includes at least one Dockerfile or docker-compose entry
       - `state_updates` key: `"infra_engineer_output"`
       - SYSTEM_PROMPT: role = infrastructure engineer (IMPL-04), generates Docker, CI/CD pipelines (GitHub Actions), Kubernetes manifests, Terraform configs, environment configuration files

    **YAML configs** -- all follow the established format:
    - `frontend_dev.yaml`: agent_type FRONTEND_DEV, model claude-sonnet-4, temperature 0.2, timeout 900, settings include `use_worktree: true`, `target_frameworks: ["react", "next"]`
    - `mobile_dev.yaml`: agent_type MOBILE_DEV, model claude-sonnet-4, temperature 0.2, timeout 900, settings include `use_worktree: true`, `target_platforms: ["react-native", "flutter", "native"]`
    - `infra_engineer.yaml`: agent_type INFRA_ENGINEER, model claude-sonnet-4, temperature 0.1, timeout 600, settings include `use_worktree: true`, `target_infra: ["docker", "kubernetes", "github-actions"]`

    **tests/unit/agents/test_implementation_agents.py** -- Create the test file with initial tests for these 3 agents:
    - For each of FrontendDev, MobileDev, InfraEngineer:
      - `test_{name}_agent_type`: correct AgentType value
      - `test_{name}_extends_base_agent`: assert `isinstance(agent, BaseAgent)`
      - `test_{name}_use_worktree_true`: assert `agent.use_worktree is True`
      - `test_{name}_review_passes_with_generated_files`: pass `{"generated_files": [{"path": "x", "content": "y"}]}`, assert review_passed True
      - `test_{name}_review_fails_with_empty_files`: pass `{"generated_files": []}`, assert review_passed False
    - `test_yaml_configs_load`: load all 3 YAML configs, verify agent_type values
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/unit/agents/test_implementation_agents.py -k "frontend or mobile or infra" -x -v --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/server/src/codebot/agents/frontend_dev.py` contains `class FrontendDevAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent` and `use_worktree: bool = True`
    - File `apps/server/src/codebot/agents/mobile_dev.py` contains `class MobileDevAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent` and `use_worktree: bool = True`
    - File `apps/server/src/codebot/agents/infra_engineer.py` contains `class InfraEngineerAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent` and `use_worktree: bool = True`
    - Each agent file contains `async def perceive`, `async def reason`, `async def act`, `async def review`
    - Files `configs/agents/frontend_dev.yaml`, `configs/agents/mobile_dev.yaml`, `configs/agents/infra_engineer.yaml` exist with correct agent_type values
    - Tests pass via pytest
  </acceptance_criteria>
  <done>Three primary S5 agents (Frontend, Mobile, Infrastructure) extend BaseAgent with worktree isolation fields and YAML configs. Tests pass via pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Middleware Dev and Integrations agents, complete S5 test suite</name>
  <files>
    apps/server/src/codebot/agents/middleware_dev.py,
    apps/server/src/codebot/agents/integrations.py,
    configs/agents/middleware_dev.yaml,
    configs/agents/integrations.yaml,
    tests/unit/agents/test_implementation_agents.py
  </files>
  <read_first>
    apps/server/src/codebot/agents/registry.py,
    apps/server/src/codebot/agents/frontend_dev.py,
    libs/agent-sdk/src/agent_sdk/agents/base.py,
    libs/agent-sdk/src/agent_sdk/models/enums.py,
    tests/unit/agents/conftest.py
  </read_first>
  <action>
    All agents MUST: `from agent_sdk.agents.base import BaseAgent` and use `class XxxAgent(BaseAgent)`.

    **1. middleware_dev.py** -- `@register_agent(AgentType.MIDDLEWARE_DEV)`, class `MiddlewareDevAgent(BaseAgent)`:
       - `tools: ["file_read", "file_write", "file_edit", "bash", "api_client_generator", "auth_middleware"]`
       - `use_worktree: bool = True`
       - `perceive`: extracts `planner_output`, `architect_output`, `api_designer_output`, `database_output`
       - `act`: returns `{"generated_files": [...], "middleware_stack": ..., "api_routes": ..., "auth_config": ...}`
       - `review`: validates `generated_files` is non-empty
       - `state_updates` key: `"middleware_dev_output"`
       - SYSTEM_PROMPT: role = middleware/backend developer, generates API middleware, authentication layers, request/response pipelines, database query layers

    **2. integrations.py** -- `@register_agent(AgentType.INTEGRATION_ADAPTER)`, class `IntegrationsAgent(BaseAgent)`:
       - `tools: ["file_read", "file_write", "bash", "api_client_generator", "sdk_wrapper"]`
       - `use_worktree: bool = True`
       - `perceive`: extracts `planner_output`, `architect_output`, `api_designer_output`
       - `act`: returns `{"generated_files": [...], "api_clients": ..., "webhook_handlers": ..., "sdk_wrappers": ...}`
       - `review`: validates `generated_files` is non-empty
       - `state_updates` key: `"integrations_output"`
       - SYSTEM_PROMPT: role = integrations adapter, generates third-party API client code, webhook handlers, SDK wrappers

    **YAML configs:**
    - `middleware_dev.yaml`: agent_type MIDDLEWARE_DEV, model claude-sonnet-4, temperature 0.2, timeout 900, `use_worktree: true`
    - `integrations.yaml`: agent_type INTEGRATION_ADAPTER, model claude-sonnet-4, temperature 0.2, timeout 600, `use_worktree: true`

    **Update tests/unit/agents/test_implementation_agents.py** to add tests for MiddlewareDev and Integrations:
    - For each of MiddlewareDev, Integrations:
      - `test_{name}_agent_type`: correct AgentType value
      - `test_{name}_extends_base_agent`: assert `isinstance(agent, BaseAgent)`
      - `test_{name}_use_worktree_true`: assert `agent.use_worktree is True`
      - `test_{name}_review_passes_with_generated_files`: pass `{"generated_files": [{"path": "x", "content": "y"}]}`, assert review_passed True
      - `test_{name}_review_fails_with_empty_files`: pass `{"generated_files": []}`, assert review_passed False
    - `test_all_s5_agents_write_different_state_keys`: instantiate all 5, verify distinct state_updates keys (frontend_dev_output, mobile_dev_output, infra_engineer_output, middleware_dev_output, integrations_output)
    - `test_yaml_configs_load`: load all 5 YAML configs, verify agent_type values
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/unit/agents/test_implementation_agents.py -x -v --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/server/src/codebot/agents/middleware_dev.py` contains `class MiddlewareDevAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent`
    - File `apps/server/src/codebot/agents/integrations.py` contains `class IntegrationsAgent(BaseAgent)` and `from agent_sdk.agents.base import BaseAgent`
    - All 5 S5 agents have `use_worktree: bool = True`
    - All 5 S5 agents write to distinct state_updates keys
    - Files `configs/agents/middleware_dev.yaml` and `configs/agents/integrations.yaml` exist
    - All tests in `test_implementation_agents.py` pass
  </acceptance_criteria>
  <done>All 5 S5 Implementation agents extend BaseAgent with worktree isolation, parallel-safe state namespaces, YAML configs, and passing tests.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/v13478/Desktop/CodeBot && uv run pytest tests/unit/agents/test_implementation_agents.py -x -v --timeout=30
```
All S5 agents extend BaseAgent, are instantiable, worktree-enabled, parallel-safe.
</verification>

<success_criteria>
1. All 5 S5 Implementation agents (Frontend, Mobile, Infrastructure, Middleware, Integrations) extend BaseAgent
2. All agents have `use_worktree = True` for worktree isolation
3. All agents write to separate SharedState keys (no parallel conflicts)
4. Frontend agent generates React/TypeScript code (IMPL-01)
5. Mobile agent generates cross-platform code (IMPL-03)
6. Infrastructure agent generates Docker/CI-CD/config (IMPL-04)
7. All YAML configs load and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-full-agent-roster/09-03-SUMMARY.md`
</output>
