---
phase: 04-multi-llm-abstraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/pyproject.toml
  - apps/server/src/codebot/llm/__init__.py
  - apps/server/src/codebot/llm/schemas.py
  - apps/server/src/codebot/llm/exceptions.py
  - apps/server/src/codebot/llm/config.py
  - apps/server/src/codebot/llm/router.py
  - apps/server/src/codebot/llm/providers.py
  - apps/server/src/codebot/config.py
  - configs/providers/llm.yaml
  - libs/agent-sdk/src/agent_sdk/models/enums.py
  - apps/server/tests/unit/__init__.py
  - apps/server/tests/unit/llm/__init__.py
  - apps/server/tests/unit/llm/conftest.py
  - apps/server/tests/unit/llm/test_schemas.py
  - apps/server/tests/unit/llm/test_config.py
  - apps/server/tests/unit/llm/test_router.py
autonomous: true
requirements:
  - LLM-01
  - LLM-02
  - LLM-06

must_haves:
  truths:
    - "TaskType enum maps every CodeBot task category to a model selection"
    - "YAML config loads provider definitions and routing rules at startup"
    - "Router selects correct model for each task type from the routing table"
    - "Router downgrades model for low-complexity tasks and keeps premium for high-complexity"
    - "Air-gapped config disables all cloud providers and routes only to self-hosted models"
    - "All LLM-related Pydantic schemas validate and serialize correctly"
  artifacts:
    - path: "apps/server/src/codebot/llm/schemas.py"
      provides: "LLMRequest, LLMResponse, TokenUsage, TaskType, RoutingConstraints, RoutingRule, BudgetDecision"
      contains: "class TaskType"
    - path: "apps/server/src/codebot/llm/config.py"
      provides: "LLMConfig loading from YAML with provider definitions and routing table"
      contains: "class LLMConfig"
    - path: "apps/server/src/codebot/llm/router.py"
      provides: "TaskBasedModelRouter with complexity and cost adjustments"
      contains: "class TaskBasedModelRouter"
    - path: "apps/server/src/codebot/llm/exceptions.py"
      provides: "BudgetExceededError, AllProvidersFailedError, ModelNotFoundError"
      contains: "class BudgetExceededError"
    - path: "apps/server/src/codebot/llm/providers.py"
      provides: "Provider registry and health status tracking"
      contains: "class ProviderRegistry"
    - path: "configs/providers/llm.yaml"
      provides: "Default LLM provider config with Anthropic, OpenAI, Google, Ollama models and routing table"
      contains: "routing_table"
  key_links:
    - from: "apps/server/src/codebot/llm/config.py"
      to: "configs/providers/llm.yaml"
      via: "YAML loading with Path resolution"
      pattern: "yaml\\.safe_load"
    - from: "apps/server/src/codebot/llm/router.py"
      to: "apps/server/src/codebot/llm/schemas.py"
      via: "imports TaskType, RoutingRule, RoutingConstraints"
      pattern: "from codebot\\.llm\\.schemas import"
---

<objective>
Create the foundation types, configuration, and task-based routing for the Multi-LLM abstraction layer.

Purpose: Establish all Pydantic schemas, custom exceptions, YAML configuration loading, and the TaskBasedModelRouter that every subsequent LLM component depends on. This plan creates the type contracts and routing logic that Plan 02 wires into the full LLMService facade.

Output: `apps/server/src/codebot/llm/` package with schemas, config, router, exceptions, providers; `configs/providers/llm.yaml` default config; test suite covering schemas, config loading, and routing.
</objective>

<execution_context>
@/Users/v13478/.claude/get-shit-done/workflows/execute-plan.md
@/Users/v13478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-llm-abstraction/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from the existing codebase. -->

From apps/server/src/codebot/config.py:
```python
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="CODEBOT_", env_file=".env", env_file_encoding="utf-8", case_sensitive=False, extra="ignore")
    database_url: str = "postgresql+asyncpg://codebot:codebot_dev@localhost:5433/codebot"
    redis_url: str = "redis://localhost:6379/0"
    nats_url: str = "nats://localhost:4222"
    lancedb_path: str = "data/lancedb"
    debug: bool = True
    log_level: str = "INFO"

settings = Settings()
```

From apps/server/src/codebot/events/bus.py:
```python
class EventBus:
    async def publish(self, event_type: str, payload: bytes) -> None: ...
    async def subscribe(self, event_type: str = ">", durable: str | None = None) -> ...: ...
```

From libs/agent-sdk/src/agent_sdk/models/enums.py:
```python
class EventType(str, enum.Enum):
    AGENT_STARTED = "AGENT_STARTED"
    # ... many event types ...
    # NOTE: No LLM-specific events exist yet. Must add LLM_USAGE, LLM_FAILURE, BUDGET_WARNING, BUDGET_EXCEEDED.
```

From libs/agent-sdk/src/agent_sdk/models/events.py:
```python
class EventEnvelope(BaseModel):
    event_type: EventType
    source_agent_id: uuid.UUID | None = None
    payload: bytes
    timestamp: datetime = ...
```

From apps/server/pyproject.toml:
```toml
dependencies = [
    "fastapi>=0.115.0", "uvicorn[standard]>=0.30.0",
    "sqlalchemy[asyncio]>=2.0.35", "asyncpg>=0.30.0", "alembic>=1.14.0",
    "pydantic>=2.9.0", "pydantic-settings>=2.5.0",
    "nats-py>=2.9.0", "redis[hiredis]>=5.0.0",
]
# NOTE: litellm, langfuse, tiktoken, pyyaml NOT yet listed. Must add them.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, create LLM schemas, exceptions, and event types</name>
  <files>
    apps/server/pyproject.toml,
    apps/server/src/codebot/llm/__init__.py,
    apps/server/src/codebot/llm/schemas.py,
    apps/server/src/codebot/llm/exceptions.py,
    libs/agent-sdk/src/agent_sdk/models/enums.py,
    apps/server/tests/unit/__init__.py,
    apps/server/tests/unit/llm/__init__.py,
    apps/server/tests/unit/llm/conftest.py,
    apps/server/tests/unit/llm/test_schemas.py
  </files>
  <read_first>
    apps/server/pyproject.toml,
    apps/server/src/codebot/config.py,
    libs/agent-sdk/src/agent_sdk/models/enums.py,
    libs/agent-sdk/src/agent_sdk/models/events.py,
    apps/server/tests/conftest.py
  </read_first>
  <behavior>
    - Test: TaskType enum has at least these values: ORCHESTRATION, CODE_GENERATION, CODE_REVIEW, RESEARCH, SIMPLE_TRANSFORM, DOCUMENTATION, TESTING, DEBUGGING, BRAINSTORMING, ARCHITECTURE, PLANNING, SECURITY_SCAN
    - Test: LLMRequest validates with messages list, optional temperature (default 0.7), optional max_tokens (default 4096), optional stream (default False)
    - Test: LLMResponse contains model (str), content (str), usage (TokenUsage), latency_ms (float)
    - Test: TokenUsage contains prompt_tokens (int), completion_tokens (int), total_tokens (int), cost_usd (float)
    - Test: RoutingRule contains primary_model (str), fallback_models (list[str]), reason (str)
    - Test: RoutingConstraints contains optional complexity_score (float | None), optional max_cost_per_call (float | None), optional prefer_local (bool, default False)
    - Test: BudgetDecision contains allowed (bool), remaining (float)
    - Test: BudgetExceededError stores agent_id and budget_decision
    - Test: AllProvidersFailedError stores model name and list of errors
    - Test: ModelNotFoundError stores model name
    - Test: EventType enum in agent_sdk contains LLM_USAGE, LLM_FAILURE, BUDGET_WARNING, BUDGET_EXCEEDED
  </behavior>
  <action>
    1. Add dependencies to `apps/server/pyproject.toml` `dependencies` list:
       - `"litellm>=1.82.0"`
       - `"langfuse>=2.0.0"`
       - `"tiktoken>=0.7.0"`
       - `"pyyaml>=6.0.0"`

    2. Run `cd /Users/v13478/Desktop/CodeBot/apps/server && uv sync` to install dependencies.

    3. Create `apps/server/src/codebot/llm/__init__.py`:
       ```python
       """Multi-LLM abstraction layer for CodeBot.

       Provides a provider-agnostic interface for all LLM calls with
       task-based routing, fallback chains, cost tracking, and budget management.
       """
       ```

    4. Create `apps/server/src/codebot/llm/schemas.py` with these Pydantic v2 models and enums:
       - `TaskType(str, enum.Enum)` with values: ORCHESTRATION, CODE_GENERATION, CODE_REVIEW, RESEARCH, SIMPLE_TRANSFORM, DOCUMENTATION, TESTING, DEBUGGING, BRAINSTORMING, ARCHITECTURE, PLANNING, SECURITY_SCAN
       - `RoutingConstraints(BaseModel)` with fields: `complexity_score: float | None = None`, `max_cost_per_call: float | None = None`, `prefer_local: bool = False`
       - `RoutingRule(BaseModel)` with fields: `primary_model: str`, `fallback_models: list[str] = []`, `reason: str = ""`
       - `LLMMessage(BaseModel)` with fields: `role: str`, `content: str`
       - `LLMRequest(BaseModel)` with fields: `messages: list[LLMMessage]`, `temperature: float = 0.7`, `max_tokens: int = 4096`, `stream: bool = False`, `constraints: RoutingConstraints | None = None`
       - `TokenUsage(BaseModel)` with fields: `prompt_tokens: int = 0`, `completion_tokens: int = 0`, `total_tokens: int = 0`, `cost_usd: float = 0.0`
       - `LLMResponse(BaseModel)` with fields: `model: str`, `content: str`, `usage: TokenUsage`, `latency_ms: float = 0.0`
       - `BudgetDecision(BaseModel)` with fields: `allowed: bool`, `remaining: float`
       - Use `model_config = ConfigDict(frozen=True)` on immutable models (TokenUsage, BudgetDecision, RoutingRule, LLMResponse).
       - Use `slots=True` and `kw_only=True` on dataclasses per CLAUDE.md convention (but these are Pydantic models, so use `model_config` instead).

    5. Create `apps/server/src/codebot/llm/exceptions.py`:
       - `class LLMError(Exception)` -- base class
       - `class BudgetExceededError(LLMError)` with `__init__(self, agent_id: str, budget_decision: BudgetDecision)` storing both as attributes
       - `class AllProvidersFailedError(LLMError)` with `__init__(self, model: str, errors: list[Exception])` storing both as attributes
       - `class ModelNotFoundError(LLMError)` with `__init__(self, model: str)` storing model as attribute
       - `class ProviderUnavailableError(LLMError)` with `__init__(self, provider: str, reason: str)` storing both

    6. Add LLM event types to `libs/agent-sdk/src/agent_sdk/models/enums.py` in the `EventType` enum:
       - `LLM_USAGE = "LLM_USAGE"`
       - `LLM_FAILURE = "LLM_FAILURE"`
       - `BUDGET_WARNING = "BUDGET_WARNING"`
       - `BUDGET_EXCEEDED = "BUDGET_EXCEEDED"`

    7. Create test directories and files:
       - `apps/server/tests/unit/__init__.py` (empty)
       - `apps/server/tests/unit/llm/__init__.py` (empty)
       - `apps/server/tests/unit/llm/conftest.py` with shared fixtures:
         - `sample_messages` fixture returning a list of `LLMMessage` objects
         - `sample_routing_rule` fixture returning a `RoutingRule`
         - `mock_litellm_response` fixture returning a mock LiteLLM ModelResponse object with usage fields
       - `apps/server/tests/unit/llm/test_schemas.py` testing all schema construction, validation, serialization, and defaults
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot/apps/server && uv run pytest tests/unit/llm/test_schemas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - apps/server/pyproject.toml contains "litellm>=1.82.0"
    - apps/server/pyproject.toml contains "langfuse>=2.0.0"
    - apps/server/pyproject.toml contains "tiktoken>=0.7.0"
    - apps/server/pyproject.toml contains "pyyaml>=6.0.0"
    - apps/server/src/codebot/llm/schemas.py contains "class TaskType(str, enum.Enum)"
    - apps/server/src/codebot/llm/schemas.py contains "class LLMRequest(BaseModel)"
    - apps/server/src/codebot/llm/schemas.py contains "class LLMResponse(BaseModel)"
    - apps/server/src/codebot/llm/schemas.py contains "class TokenUsage(BaseModel)"
    - apps/server/src/codebot/llm/schemas.py contains "class RoutingRule(BaseModel)"
    - apps/server/src/codebot/llm/schemas.py contains "class RoutingConstraints(BaseModel)"
    - apps/server/src/codebot/llm/schemas.py contains "class BudgetDecision(BaseModel)"
    - apps/server/src/codebot/llm/exceptions.py contains "class BudgetExceededError(LLMError)"
    - apps/server/src/codebot/llm/exceptions.py contains "class AllProvidersFailedError(LLMError)"
    - apps/server/src/codebot/llm/exceptions.py contains "class ModelNotFoundError(LLMError)"
    - libs/agent-sdk/src/agent_sdk/models/enums.py contains "LLM_USAGE"
    - libs/agent-sdk/src/agent_sdk/models/enums.py contains "BUDGET_EXCEEDED"
    - uv run pytest tests/unit/llm/test_schemas.py exits 0
  </acceptance_criteria>
  <done>All LLM Pydantic schemas validate and serialize, all custom exceptions instantiate with correct attributes, EventType enum includes LLM events, test suite passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create YAML config loading and provider registry</name>
  <files>
    apps/server/src/codebot/llm/config.py,
    apps/server/src/codebot/llm/providers.py,
    apps/server/src/codebot/config.py,
    configs/providers/llm.yaml,
    apps/server/tests/unit/llm/test_config.py
  </files>
  <read_first>
    apps/server/src/codebot/llm/schemas.py,
    apps/server/src/codebot/llm/exceptions.py,
    apps/server/src/codebot/config.py,
    configs/providers/llm.yaml
  </read_first>
  <behavior>
    - Test: LLMConfig loads from a YAML file containing model_list, routing_table, budget, and provider settings
    - Test: ProviderConfig validates model_name (str), litellm_model (str), api_key_env (str | None), api_base (str | None), enabled (bool, default True)
    - Test: RoutingTableConfig maps TaskType values to RoutingRule objects
    - Test: BudgetConfig contains global_budget_usd (float), warn_threshold (float, default 0.8), halt_threshold (float, default 0.95), agent_budgets (dict)
    - Test: LLMConfig.from_yaml() loads a valid YAML file and returns a populated LLMConfig
    - Test: Air-gapped config (all cloud providers have enabled=false) loads correctly and only self-hosted models are available
    - Test: Missing YAML file raises FileNotFoundError
    - Test: Invalid YAML content raises ValueError with descriptive message
    - Test: ProviderRegistry.get_enabled_providers() returns only providers with enabled=True
    - Test: ProviderRegistry.is_provider_healthy(name) returns health status
    - Test: Settings class has llm_config_path field defaulting to "configs/providers/llm.yaml"
  </behavior>
  <action>
    1. Create `configs/providers/llm.yaml` with this exact content:
       ```yaml
       # CodeBot LLM Provider Configuration
       # Loaded by apps/server/src/codebot/llm/config.py

       providers:
         - model_name: "claude-opus"
           litellm_model: "anthropic/claude-opus-4"
           api_key_env: "ANTHROPIC_API_KEY"
           enabled: true

         - model_name: "claude-sonnet"
           litellm_model: "anthropic/claude-sonnet-4"
           api_key_env: "ANTHROPIC_API_KEY"
           enabled: true

         - model_name: "claude-haiku"
           litellm_model: "anthropic/claude-haiku-3.5"
           api_key_env: "ANTHROPIC_API_KEY"
           enabled: true

         - model_name: "gpt-4o"
           litellm_model: "openai/gpt-4o"
           api_key_env: "OPENAI_API_KEY"
           enabled: true

         - model_name: "gpt-4o-mini"
           litellm_model: "openai/gpt-4o-mini"
           api_key_env: "OPENAI_API_KEY"
           enabled: true

         - model_name: "gemini-pro"
           litellm_model: "gemini/gemini-2.5-pro"
           api_key_env: "GOOGLE_API_KEY"
           enabled: true

         - model_name: "gemini-flash"
           litellm_model: "gemini/gemini-2.5-flash"
           api_key_env: "GOOGLE_API_KEY"
           enabled: true

         - model_name: "ollama-llama"
           litellm_model: "ollama/llama3.1:70b"
           api_base: "http://localhost:11434"
           enabled: true

       routing_table:
         ORCHESTRATION:
           primary_model: "claude-opus"
           fallback_models: ["gpt-4o", "gemini-pro"]
           reason: "Complex planning and reasoning"
         CODE_GENERATION:
           primary_model: "claude-sonnet"
           fallback_models: ["gpt-4o", "gemini-pro"]
           reason: "Fast, high-quality code generation"
         CODE_REVIEW:
           primary_model: "claude-opus"
           fallback_models: ["gpt-4o"]
           reason: "Nuanced code understanding"
         RESEARCH:
           primary_model: "gemini-pro"
           fallback_models: ["claude-opus"]
           reason: "Large context window for research synthesis"
         SIMPLE_TRANSFORM:
           primary_model: "claude-haiku"
           fallback_models: ["gemini-flash", "gpt-4o-mini"]
           reason: "Fast, cheap transformations"
         DOCUMENTATION:
           primary_model: "claude-sonnet"
           fallback_models: ["gpt-4o", "gemini-pro"]
           reason: "Clear technical writing"
         TESTING:
           primary_model: "claude-sonnet"
           fallback_models: ["gpt-4o"]
           reason: "Test generation with good coverage"
         DEBUGGING:
           primary_model: "claude-opus"
           fallback_models: ["gpt-4o", "claude-sonnet"]
           reason: "Deep reasoning for root cause analysis"
         BRAINSTORMING:
           primary_model: "claude-opus"
           fallback_models: ["gemini-pro", "gpt-4o"]
           reason: "Creative ideation"
         ARCHITECTURE:
           primary_model: "claude-opus"
           fallback_models: ["gpt-4o", "gemini-pro"]
           reason: "System design expertise"
         PLANNING:
           primary_model: "claude-sonnet"
           fallback_models: ["gpt-4o"]
           reason: "Task decomposition"
         SECURITY_SCAN:
           primary_model: "claude-sonnet"
           fallback_models: ["gpt-4o"]
           reason: "Security pattern recognition"

       budget:
         global_budget_usd: 50.0
         warn_threshold: 0.8
         halt_threshold: 0.95
         agent_budgets: {}

       fallback:
         num_retries: 3
         timeout_seconds: 60
         cooldown_seconds: 300

       settings:
         default_temperature: 0.7
         default_max_tokens: 4096
         enable_streaming: true
         enable_cost_tracking: true
       ```

    2. Create `apps/server/src/codebot/llm/config.py`:
       - `class ProviderConfig(BaseModel)` with: `model_name: str`, `litellm_model: str`, `api_key_env: str | None = None`, `api_base: str | None = None`, `enabled: bool = True`
       - `class FallbackConfig(BaseModel)` with: `num_retries: int = 3`, `timeout_seconds: int = 60`, `cooldown_seconds: int = 300`
       - `class BudgetConfig(BaseModel)` with: `global_budget_usd: float = 50.0`, `warn_threshold: float = 0.8`, `halt_threshold: float = 0.95`, `agent_budgets: dict[str, float] = {}`
       - `class LLMSettings(BaseModel)` with: `default_temperature: float = 0.7`, `default_max_tokens: int = 4096`, `enable_streaming: bool = True`, `enable_cost_tracking: bool = True`
       - `class LLMConfig(BaseModel)` with: `providers: list[ProviderConfig]`, `routing_table: dict[str, RoutingRule]`, `budget: BudgetConfig = BudgetConfig()`, `fallback: FallbackConfig = FallbackConfig()`, `settings: LLMSettings = LLMSettings()`
       - Class method `LLMConfig.from_yaml(path: str | Path) -> LLMConfig` that reads the YAML, validates with Pydantic, raises `FileNotFoundError` if missing, raises `ValueError` on invalid content
       - Method `LLMConfig.get_enabled_providers() -> list[ProviderConfig]` returning only `enabled=True`
       - Method `LLMConfig.get_routing_rule(task_type: str) -> RoutingRule` with fallback to a default rule using the first enabled provider

    3. Create `apps/server/src/codebot/llm/providers.py`:
       - `class ProviderHealth(BaseModel)` with: `name: str`, `healthy: bool = True`, `last_error: str | None = None`, `consecutive_failures: int = 0`, `last_checked: datetime | None = None`
       - `class ProviderRegistry` with:
         - `__init__(self, config: LLMConfig)` building internal provider map from config
         - `get_enabled_providers() -> list[ProviderConfig]`
         - `get_provider(name: str) -> ProviderConfig` raising `ModelNotFoundError` if not found
         - `is_provider_healthy(name: str) -> bool`
         - `record_failure(name: str, error: str) -> None` incrementing consecutive_failures
         - `record_success(name: str) -> None` resetting consecutive_failures
         - `build_litellm_model_list() -> list[dict]` returning the list of dicts that LiteLLM Router expects (model_name, litellm_params with model, api_key from os.getenv, api_base)

    4. Update `apps/server/src/codebot/config.py` to add:
       - `llm_config_path: str = "configs/providers/llm.yaml"` field in Settings class

    5. Create `apps/server/tests/unit/llm/test_config.py` testing:
       - YAML loading from a valid file (use tmp_path fixture to create test YAML)
       - Air-gapped config (all cloud providers disabled)
       - Missing YAML file raises FileNotFoundError
       - Invalid YAML raises ValueError
       - get_enabled_providers returns only enabled providers
       - get_routing_rule returns correct rule for known task types
       - get_routing_rule returns default rule for unknown task types
       - ProviderRegistry.build_litellm_model_list returns correct dict structure
       - ProviderRegistry health tracking (record_failure, record_success, is_provider_healthy)
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot/apps/server && uv run pytest tests/unit/llm/test_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - configs/providers/llm.yaml contains "routing_table:"
    - configs/providers/llm.yaml contains "claude-opus"
    - configs/providers/llm.yaml contains "ollama-llama"
    - apps/server/src/codebot/llm/config.py contains "class LLMConfig(BaseModel)"
    - apps/server/src/codebot/llm/config.py contains "def from_yaml"
    - apps/server/src/codebot/llm/config.py contains "class ProviderConfig(BaseModel)"
    - apps/server/src/codebot/llm/config.py contains "class BudgetConfig(BaseModel)"
    - apps/server/src/codebot/llm/providers.py contains "class ProviderRegistry"
    - apps/server/src/codebot/llm/providers.py contains "def build_litellm_model_list"
    - apps/server/src/codebot/config.py contains "llm_config_path"
    - uv run pytest tests/unit/llm/test_config.py exits 0
  </acceptance_criteria>
  <done>YAML config loads all provider definitions and routing rules, ProviderRegistry tracks health and builds LiteLLM model lists, air-gapped mode works by disabling cloud providers, Settings has llm_config_path</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create TaskBasedModelRouter with complexity and cost adjustments</name>
  <files>
    apps/server/src/codebot/llm/router.py,
    apps/server/tests/unit/llm/test_router.py
  </files>
  <read_first>
    apps/server/src/codebot/llm/schemas.py,
    apps/server/src/codebot/llm/config.py,
    apps/server/src/codebot/llm/providers.py,
    apps/server/src/codebot/llm/exceptions.py
  </read_first>
  <behavior>
    - Test: route(TaskType.ORCHESTRATION) returns "claude-opus" with default config
    - Test: route(TaskType.CODE_GENERATION) returns "claude-sonnet" with default config
    - Test: route(TaskType.RESEARCH) returns "gemini-pro" with default config
    - Test: route(TaskType.SIMPLE_TRANSFORM) returns "claude-haiku" with default config
    - Test: route with complexity_score < 0.3 downgrades to a cheaper model (e.g., ORCHESTRATION with low complexity returns a cheaper model like "claude-sonnet" instead of "claude-opus")
    - Test: route with complexity_score >= 0.7 returns primary model even if downgrade would normally apply
    - Test: route with max_cost_per_call constraint returns cheapest model that meets the threshold
    - Test: route with prefer_local=True returns a self-hosted model (e.g., "ollama-llama")
    - Test: route for unknown task type uses default routing rule
    - Test: get_fallback_chain returns ordered list of fallback models for a given task type
    - Test: Router constructed from LLMConfig correctly loads routing table
  </behavior>
  <action>
    Create `apps/server/src/codebot/llm/router.py` with:

    1. `class ModelTier(str, enum.Enum)` with values: PREMIUM ("premium"), STANDARD ("standard"), ECONOMY ("economy") for model classification.

    2. `MODEL_TIER_MAP: dict[str, ModelTier]` mapping known model names to tiers:
       - PREMIUM: "claude-opus", "gpt-4o", "gemini-pro"
       - STANDARD: "claude-sonnet", "gpt-4o-mini", "gemini-flash"
       - ECONOMY: "claude-haiku", "ollama-llama"

    3. `DOWNGRADE_MAP: dict[str, str]` mapping premium models to their standard equivalents:
       - "claude-opus" -> "claude-sonnet"
       - "gpt-4o" -> "gpt-4o-mini"
       - "gemini-pro" -> "gemini-flash"

    4. `class TaskBasedModelRouter`:
       - `__init__(self, config: LLMConfig, provider_registry: ProviderRegistry)` storing routing table from config and the registry for health checks
       - `route(self, task_type: TaskType, constraints: RoutingConstraints | None = None) -> str`:
         - Look up routing rule by task_type.value in routing table
         - If no rule found, use first enabled provider as default
         - If `constraints.prefer_local` is True, return first self-hosted model (check for models with api_base set or model names starting with "ollama")
         - If `constraints.complexity_score is not None and constraints.complexity_score < 0.3`, call `_downgrade_model(rule.primary_model)` returning the cheaper equivalent
         - If `constraints.complexity_score is not None and constraints.complexity_score >= 0.7`, return `rule.primary_model` (ensure premium)
         - If `constraints.max_cost_per_call is not None`, call `_find_cheapest_capable(task_type, constraints.max_cost_per_call)`
         - Default: return `rule.primary_model`
       - `get_fallback_chain(self, task_type: TaskType) -> list[str]`:
         - Returns `[rule.primary_model] + rule.fallback_models` for the task type
       - `_downgrade_model(self, model: str) -> str`:
         - Look up in DOWNGRADE_MAP, return downgraded model if found, otherwise return original
       - `_find_cheapest_capable(self, task_type: TaskType, max_cost: float) -> str`:
         - Sort available models by tier (ECONOMY first, then STANDARD, then PREMIUM)
         - Return first model that is enabled and healthy
         - If none found, return primary model from routing rule (best-effort)
       - `_get_local_models(self) -> list[str]`:
         - Return model names where provider has `api_base` set (self-hosted) or litellm_model starts with "ollama/"

    5. Create `apps/server/tests/unit/llm/test_router.py` with:
       - Fixture that creates an LLMConfig from a test YAML string (using tmp_path)
       - Fixture that creates a ProviderRegistry from that config
       - Fixture that creates a TaskBasedModelRouter from config + registry
       - Tests for all behaviors listed above
       - Test that router handles missing routing rule gracefully (unknown task type)
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot/apps/server && uv run pytest tests/unit/llm/test_router.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - apps/server/src/codebot/llm/router.py contains "class TaskBasedModelRouter"
    - apps/server/src/codebot/llm/router.py contains "def route(self, task_type: TaskType"
    - apps/server/src/codebot/llm/router.py contains "def get_fallback_chain"
    - apps/server/src/codebot/llm/router.py contains "_downgrade_model"
    - apps/server/src/codebot/llm/router.py contains "_find_cheapest_capable"
    - apps/server/src/codebot/llm/router.py contains "class ModelTier"
    - apps/server/src/codebot/llm/router.py contains "DOWNGRADE_MAP"
    - uv run pytest tests/unit/llm/test_router.py exits 0
  </acceptance_criteria>
  <done>TaskBasedModelRouter selects correct model per task type, downgrades for low complexity, keeps premium for high complexity, finds cheapest model under cost constraints, returns local models when prefer_local is True, provides fallback chains</done>
</task>

</tasks>

<verification>
Run the full LLM test suite to confirm all Plan 01 components work together:
```bash
cd /Users/v13478/Desktop/CodeBot/apps/server && uv run pytest tests/unit/llm/ -x -q
```

Verify type checking passes:
```bash
cd /Users/v13478/Desktop/CodeBot/apps/server && uv run mypy src/codebot/llm/ --strict --ignore-missing-imports
```
</verification>

<success_criteria>
1. All 3 test files pass: test_schemas.py, test_config.py, test_router.py
2. LLM schemas define all types needed by Plan 02 (LLMRequest, LLMResponse, TokenUsage, TaskType, RoutingRule, RoutingConstraints, BudgetDecision)
3. YAML config loads and validates provider definitions and routing table
4. TaskBasedModelRouter routes all 12 task types to correct models with complexity and cost adjustments
5. Air-gapped configuration works by disabling cloud providers
6. EventType enum includes LLM_USAGE, LLM_FAILURE, BUDGET_WARNING, BUDGET_EXCEEDED
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-llm-abstraction/04-01-SUMMARY.md`
</output>
