---
phase: 01-foundation-and-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - libs/agent-sdk/src/agent_sdk/__init__.py
  - libs/agent-sdk/src/agent_sdk/models/__init__.py
  - libs/agent-sdk/src/agent_sdk/models/project.py
  - libs/agent-sdk/src/agent_sdk/models/agent.py
  - libs/agent-sdk/src/agent_sdk/models/task.py
  - libs/agent-sdk/src/agent_sdk/models/pipeline.py
  - libs/agent-sdk/src/agent_sdk/models/events.py
  - libs/agent-sdk/src/agent_sdk/models/enums.py
  - libs/shared-types/src/index.ts
  - libs/shared-types/src/enums.ts
  - libs/shared-types/src/project.ts
  - libs/shared-types/src/agent.ts
  - libs/shared-types/src/task.ts
  - libs/shared-types/src/events.ts
  - apps/server/src/codebot/events/__init__.py
  - apps/server/src/codebot/events/bus.py
  - apps/server/tests/test_events.py
  - apps/server/tests/test_models.py
autonomous: true
requirements:
  - REQ-004
  - REQ-005

must_haves:
  truths:
    - "Shared Pydantic models compile and are importable from agent_sdk package"
    - "TypeScript types compile and are importable from @codebot/shared-types package"
    - "A Python test can publish a message to NATS JetStream and a subscriber receives it within 1 second"
    - "Pydantic model field names and types match the TypeScript type definitions"
  artifacts:
    - path: "libs/agent-sdk/src/agent_sdk/models/enums.py"
      provides: "All shared enum types (ProjectStatus, AgentType, etc.)"
      exports: ["ProjectStatus", "AgentType", "TaskStatus", "PipelineStatus"]
    - path: "libs/agent-sdk/src/agent_sdk/models/project.py"
      provides: "Pydantic schemas for Project, Pipeline, PipelinePhase"
      exports: ["ProjectSchema", "PipelineSchema"]
    - path: "libs/shared-types/src/index.ts"
      provides: "Barrel export for all TypeScript types"
      min_lines: 10
    - path: "apps/server/src/codebot/events/bus.py"
      provides: "NATS JetStream event bus wrapper"
      exports: ["EventBus", "create_event_bus"]
    - path: "apps/server/tests/test_events.py"
      provides: "NATS JetStream pub/sub integration test"
      contains: "test_jetstream_pub_sub"
  key_links:
    - from: "libs/shared-types/src/enums.ts"
      to: "libs/agent-sdk/src/agent_sdk/models/enums.py"
      via: "Matching enum values (manually synced, automated generation in future)"
      pattern: "ProjectStatus|AgentType"
    - from: "apps/server/src/codebot/events/bus.py"
      to: "libs/agent-sdk/src/agent_sdk/models/events.py"
      via: "Event payloads use Pydantic models for serialization"
      pattern: "model_dump_json|model_validate_json"
---

<objective>
Create shared Pydantic models in agent-sdk, matching TypeScript types in shared-types, and implement the NATS JetStream event bus with pub/sub tests.

Purpose: Establish the shared type contracts that all agents, API endpoints, and the dashboard will use, plus the async messaging backbone for inter-agent communication.
Output: Importable Pydantic models, compilable TypeScript types, working NATS JetStream event bus with verified pub/sub.
</objective>

<execution_context>
@/Users/v13478/.claude/get-shit-done/workflows/execute-plan.md
@/Users/v13478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-scaffolding/01-RESEARCH.md
@docs/design/DATA_MODELS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared Pydantic models and TypeScript types</name>
  <files>
    libs/agent-sdk/src/agent_sdk/__init__.py
    libs/agent-sdk/src/agent_sdk/models/__init__.py
    libs/agent-sdk/src/agent_sdk/models/project.py
    libs/agent-sdk/src/agent_sdk/models/agent.py
    libs/agent-sdk/src/agent_sdk/models/task.py
    libs/agent-sdk/src/agent_sdk/models/pipeline.py
    libs/agent-sdk/src/agent_sdk/models/events.py
    libs/agent-sdk/src/agent_sdk/models/enums.py
    libs/shared-types/src/index.ts
    libs/shared-types/src/enums.ts
    libs/shared-types/src/project.ts
    libs/shared-types/src/agent.ts
    libs/shared-types/src/task.ts
    libs/shared-types/src/events.ts
    apps/server/tests/test_models.py
  </files>
  <action>
**NOTE:** These are API/event Pydantic schemas (not SQLAlchemy ORM models -- those are in Plan 01-02). These Pydantic models define the data contracts for inter-service communication, API responses, and event payloads.

**libs/agent-sdk/src/agent_sdk/models/enums.py**: Define ALL enums from DATA_MODELS.md as `str, enum.Enum` classes (StrEnum pattern for JSON serialization):
- ProjectStatus, ProjectType, PipelineStatus, PhaseType, PhaseStatus
- TaskStatus, AgentType, AgentStatus, ExecutionStatus
- TestStatus, ExperimentStatus
- FindingType, Severity, FindingStatus
- CommentType, CommentStatus, EventType

**libs/agent-sdk/src/agent_sdk/models/project.py**: Pydantic v2 BaseModel schemas:
- `ProjectSchema`: id (UUID), name (str), description (str), status (ProjectStatus), project_type (ProjectType), tech_stack (dict), created_at (datetime), updated_at (datetime). Use `model_config = ConfigDict(from_attributes=True)` for ORM compatibility.
- `PipelineSchema`: id, project_id, status, current_phase, total_tokens_used, total_cost_usd, started_at, completed_at.
- `PipelinePhaseSchema`: id, pipeline_id, name, phase_type, status, order, requires_approval, started_at, completed_at.

**libs/agent-sdk/src/agent_sdk/models/agent.py**: AgentSchema, AgentExecutionSchema with all fields from DATA_MODELS.md.

**libs/agent-sdk/src/agent_sdk/models/task.py**: TaskSchema with all fields.

**libs/agent-sdk/src/agent_sdk/models/pipeline.py**: PipelineCreateRequest, PipelineStatusResponse (API contract models).

**libs/agent-sdk/src/agent_sdk/models/events.py**: Event payload Pydantic models:
- `AgentEvent`: agent_id, agent_type, status, timestamp, payload (optional dict)
- `TaskEvent`: task_id, agent_id, status, timestamp
- `PipelineEvent`: pipeline_id, phase, status, timestamp
- `EventEnvelope`: event_type (EventType), source_agent_id (optional UUID), payload (bytes), timestamp

**libs/agent-sdk/src/agent_sdk/models/__init__.py**: Barrel imports for all schemas and enums.

**libs/agent-sdk/src/agent_sdk/__init__.py**: Re-export from models subpackage.

**libs/shared-types/src/enums.ts**: TypeScript string enums matching ALL Python enums exactly. Same member names and values.

**libs/shared-types/src/project.ts**: TypeScript interfaces: `Project`, `Pipeline`, `PipelinePhase` matching field names and types of the Pydantic schemas. Use `string` for UUID, `string` for datetime (ISO format).

**libs/shared-types/src/agent.ts**: TypeScript interfaces: `Agent`, `AgentExecution`.

**libs/shared-types/src/task.ts**: TypeScript interface: `Task`.

**libs/shared-types/src/events.ts**: TypeScript interfaces: `AgentEvent`, `TaskEvent`, `PipelineEvent`, `EventEnvelope`.

**libs/shared-types/src/index.ts**: Barrel export all types and enums using `export type` for interfaces and `export` for enums.

**Cross-language parity verification:** In `apps/server/tests/test_models.py`, include a test (`test_enum_parity_with_typescript`) that reads `libs/shared-types/src/enums.ts`, extracts all enum member names using regex, and compares them against the Python enum members from `agent_sdk.models.enums`. This ensures Python and TypeScript enums stay in sync. The test should: (1) parse the .ts file for `enum <Name> { ... }` blocks, (2) extract member names from each block, (3) assert the set of member names matches the corresponding Python enum's `__members__` keys. If the .ts file is not found (e.g., running in CI without the full monorepo), the test should be marked `pytest.mark.skipif`. Note: Full automated `pydantic-to-typescript` generation is deferred to a later phase; this test covers the critical enum parity gap for now.

**apps/server/tests/test_models.py**: Tests that:
1. All Pydantic models can be instantiated with valid data
2. Models validate required fields (missing field raises ValidationError)
3. Enum values serialize to strings correctly
4. `model_dump_json()` produces valid JSON
5. `model_validate_json()` round-trips correctly
6. Python enum members match TypeScript enum members (cross-language parity test)
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv run python -c "from agent_sdk.models import ProjectSchema, AgentEvent; print('Pydantic models OK')" && pnpm --filter @codebot/shared-types build && cd apps/server && uv run pytest tests/test_models.py -x -v --timeout=30</automated>
  </verify>
  <done>All Pydantic models importable from agent_sdk. All TypeScript types compile. Field names match between Python and TypeScript. Model tests pass with serialization round-trips. Cross-language enum parity test verifies Python and TypeScript enum members are identical.</done>
</task>

<task type="auto">
  <name>Task 2: NATS JetStream event bus with pub/sub tests</name>
  <files>
    apps/server/src/codebot/events/__init__.py
    apps/server/src/codebot/events/bus.py
    apps/server/tests/test_events.py
  </files>
  <action>
**PREREQUISITE:** NATS must be running via Docker Compose (started by Plan 01-02). If 01-02 hasn't run yet (parallel execution), start NATS independently with JetStream enabled: `docker run -d --name nats-test -p 4222:4222 -p 8222:8222 nats:latest --js` (the `--js` flag is REQUIRED for JetStream stream creation to work; without it, `add_stream` calls will fail with a "JetStream not enabled" error). The event bus code doesn't depend on PostgreSQL or Redis.

**apps/server/src/codebot/events/bus.py**: Implement EventBus class following Pattern 4 from research:

```python
class EventBus:
    """NATS JetStream event bus for inter-agent messaging."""

    def __init__(self, nats_url: str = "nats://localhost:4222"):
        self._nats_url = nats_url
        self._nc: nats.NATS | None = None
        self._js: JetStreamContext | None = None

    async def connect(self) -> None:
        """Connect to NATS and initialize JetStream context."""
        self._nc = await nats.connect(self._nats_url)
        self._js = self._nc.jetstream()
        # Create the main event stream (idempotent)
        await self._js.add_stream(
            StreamConfig(
                name="codebot-events",
                subjects=["codebot.events.>"],
                retention="limits",
                max_msgs=100_000,
            )
        )

    async def disconnect(self) -> None:
        """Drain and close NATS connection."""
        if self._nc:
            await self._nc.drain()

    async def publish(self, event_type: str, payload: bytes) -> None:
        """Publish event to JetStream."""
        subject = f"codebot.events.{event_type}"
        await self._js.publish(subject, payload)

    async def subscribe(self, event_type: str = ">", durable: str | None = None):
        """Subscribe to events. Use '>' for all events."""
        subject = f"codebot.events.{event_type}"
        if durable:
            return await self._js.subscribe(subject, durable=durable)
        return await self._js.subscribe(subject)
```

Also add a convenience function `create_event_bus(nats_url)` that creates, connects, and returns an EventBus instance.

Add a `publish_event` helper that accepts an `EventEnvelope` Pydantic model, serializes it to JSON bytes, and publishes via the bus.

**apps/server/src/codebot/events/__init__.py**: Export EventBus, create_event_bus, publish_event.

**apps/server/tests/test_events.py**: Integration test requiring NATS to be running:
1. `test_jetstream_pub_sub`: Create EventBus, connect, publish a message with `event_type="agent.started"` and a JSON payload, subscribe and receive within 1 second using `asyncio.wait_for(timeout=1.0)`, verify payload matches. Clean up stream after test.
2. `test_event_envelope_roundtrip`: Create an AgentEvent Pydantic model, serialize to JSON bytes, publish via EventBus, subscribe and deserialize back, verify all fields match.
3. `test_subscribe_filtered`: Publish events with types "agent.started" and "task.completed", subscribe only to "agent.>", verify only agent events received.

Mark all tests with `@pytest.mark.asyncio`. Add a `@pytest.mark.skipif` that skips if NATS is not reachable (try connecting, skip if ConnectionRefusedError).
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot/apps/server && uv run pytest tests/test_events.py -x -v --timeout=30</automated>
  </verify>
  <done>EventBus connects to NATS JetStream, creates streams, publishes and receives messages within 1 second. Filtered subscriptions work. Event envelope round-trips through serialization.</done>
</task>

</tasks>

<verification>
1. `uv run python -c "from agent_sdk.models import ProjectSchema, AgentType, EventEnvelope; print('OK')"` succeeds
2. `pnpm --filter @codebot/shared-types build` compiles without errors
3. `cd apps/server && uv run pytest tests/test_models.py tests/test_events.py -x -v` all pass
4. NATS JetStream pub/sub completes within 1 second (per REQ-005 success criteria)
5. Cross-language enum parity test passes (Python enum members match TypeScript enum members)
</verification>

<success_criteria>
- All Pydantic models importable from `agent_sdk.models`
- All TypeScript types compile from `@codebot/shared-types`
- Enum values match between Python and TypeScript (verified by automated parity test)
- NATS JetStream publish/subscribe round-trip within 1 second
- Event envelope serialization/deserialization works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-scaffolding/01-03-SUMMARY.md`
</output>
