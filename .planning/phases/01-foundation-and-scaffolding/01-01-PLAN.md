---
phase: 01-foundation-and-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - Makefile
  - .gitignore
  - .env.example
  - .pre-commit-config.yaml
  - apps/server/pyproject.toml
  - apps/server/package.json
  - apps/server/src/codebot/__init__.py
  - apps/server/src/codebot/main.py
  - apps/dashboard/package.json
  - apps/dashboard/tsconfig.json
  - apps/dashboard/vite.config.ts
  - apps/dashboard/src/main.tsx
  - apps/cli/package.json
  - apps/cli/tsconfig.json
  - apps/cli/src/index.ts
  - libs/agent-sdk/pyproject.toml
  - libs/agent-sdk/package.json
  - libs/agent-sdk/src/agent_sdk/__init__.py
  - libs/shared-types/package.json
  - libs/shared-types/tsconfig.json
  - libs/shared-types/src/index.ts
  - libs/graph-engine/pyproject.toml
  - libs/graph-engine/package.json
  - libs/graph-engine/src/graph_engine/__init__.py
  - configs/pipelines/.gitkeep
  - configs/agents/.gitkeep
  - configs/providers/.gitkeep
autonomous: true
requirements:
  - REQ-001

must_haves:
  truths:
    - "`uv sync` succeeds from a clean clone and installs all Python workspace packages"
    - "`pnpm install` succeeds and links all workspace packages"
    - "`turbo build` completes for all workspaces without errors"
    - "All workspace directories (apps/server, apps/dashboard, apps/cli, libs/agent-sdk, libs/shared-types, libs/graph-engine) exist with valid configs"
  artifacts:
    - path: "pyproject.toml"
      provides: "Root Python workspace config with uv workspace members"
      contains: "requires-python = \">=3.12\""
    - path: "package.json"
      provides: "Root Node.js workspace config (devDependencies only)"
      contains: "turbo"
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace declaration"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Turborepo task pipeline configuration"
      contains: "build"
    - path: "Makefile"
      provides: "Common development commands"
      contains: "dev"
  key_links:
    - from: "turbo.json"
      to: "pnpm-workspace.yaml"
      via: "Turborepo discovers workspaces from pnpm workspace config"
      pattern: "packages"
    - from: "apps/server/package.json"
      to: "pyproject.toml"
      via: "Shim scripts delegate to uv run"
      pattern: "uv run"
---

<objective>
Scaffold the complete Turborepo monorepo with all workspace directories, root configs, and package.json shims for Python packages.

Purpose: Establish the repository structure that all subsequent plans depend on -- directory layout, package managers, build tooling, and workspace linkage.
Output: Working monorepo where `uv sync`, `pnpm install`, and `turbo build` all succeed.
</objective>

<execution_context>
@/Users/v13478/.claude/get-shit-done/workflows/execute-plan.md
@/Users/v13478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-scaffolding/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root configs and workspace declarations</name>
  <files>
    pyproject.toml
    package.json
    pnpm-workspace.yaml
    turbo.json
    Makefile
    .gitignore
    .env.example
    .pre-commit-config.yaml
  </files>
  <action>
Create all root-level configuration files FROM SCRATCH (do not modify existing pyproject.toml or package.json -- they have discrepancies noted in research). Delete existing root pyproject.toml and package.json first, then create clean versions.

**pyproject.toml** (root):
- `requires-python = ">=3.12"`
- `[tool.uv.workspace]` with members: `["apps/server", "libs/agent-sdk", "libs/graph-engine"]`
- `[tool.ruff]` with `target-version = "py312"`, format and lint config
- `[tool.mypy]` with `python_version = "3.12"`, strict mode enabled
- `[tool.pytest.ini_options]` with `asyncio_mode = "auto"`, `testpaths = ["tests"]`
- No direct dependencies at root level (each package declares its own)

**package.json** (root):
- `"name": "codebot"`, `"private": true`
- `"packageManager": "pnpm@9.14.0"`
- devDependencies ONLY: `turbo`, `typescript` (no react, no next, no eslint, no yjs, no tremor)
- Scripts: `"build": "turbo build"`, `"test": "turbo test"`, `"lint": "turbo lint"`, `"typecheck": "turbo typecheck"`, `"dev": "turbo dev"`, `"format": "turbo format"`

**pnpm-workspace.yaml**: packages: `["apps/*", "libs/*", "sdks/*"]`

**turbo.json**: Task pipeline with build (dependsOn ^build, outputs dist/**), test (dependsOn build, no cache), lint (no deps), typecheck (dependsOn ^build), dev (persistent, no cache), migrate (no cache). Use `$schema` from turbo.build.

**Makefile**: Targets for `dev`, `build`, `test`, `lint`, `typecheck`, `migrate` (delegates to `uv run alembic upgrade head`), `docker-up` (`docker-compose up -d`), `docker-down`, `clean`, `install` (`uv sync && pnpm install`).

**.gitignore**: Python (__pycache__, *.pyc, .venv, dist/, *.egg-info), Node (node_modules/, dist/, .turbo/), IDE (.idea/, .vscode/), env (.env, !.env.example), Docker volumes, OS files (.DS_Store).

**.env.example**: Template with CODEBOT_DATABASE_URL, CODEBOT_REDIS_URL, CODEBOT_NATS_URL, CODEBOT_DEBUG, CODEBOT_LOG_LEVEL with default dev values as comments.

**.pre-commit-config.yaml**: Hooks for ruff (format + check) and mypy.
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && cat pyproject.toml | grep 'requires-python' && cat package.json | grep '"turbo"' && cat pnpm-workspace.yaml | grep 'apps' && cat turbo.json | grep '"build"'</automated>
  </verify>
  <done>All root config files exist with correct content. No stale dependencies (next, eslint, yjs, tremor) present. Python targets 3.12+.</done>
</task>

<task type="auto">
  <name>Task 2: Create all workspace packages with configs and stubs</name>
  <files>
    apps/server/pyproject.toml
    apps/server/package.json
    apps/server/src/codebot/__init__.py
    apps/server/src/codebot/main.py
    apps/dashboard/package.json
    apps/dashboard/tsconfig.json
    apps/dashboard/vite.config.ts
    apps/dashboard/src/main.tsx
    apps/cli/package.json
    apps/cli/tsconfig.json
    apps/cli/src/index.ts
    libs/agent-sdk/pyproject.toml
    libs/agent-sdk/package.json
    libs/agent-sdk/src/agent_sdk/__init__.py
    libs/shared-types/package.json
    libs/shared-types/tsconfig.json
    libs/shared-types/src/index.ts
    libs/graph-engine/pyproject.toml
    libs/graph-engine/package.json
    libs/graph-engine/src/graph_engine/__init__.py
    configs/pipelines/.gitkeep
    configs/agents/.gitkeep
    configs/providers/.gitkeep
  </files>
  <action>
Create all workspace directories and their config files.

**apps/server/** (Python, FastAPI):
- `pyproject.toml`: name `codebot-server`, python >=3.12, dependencies: fastapi, uvicorn[standard], sqlalchemy[asyncio]>=2.0.35, asyncpg>=0.30.0, alembic>=1.14.0, pydantic>=2.9.0, pydantic-settings>=2.5.0, nats-py>=2.9.0, redis[hiredis]>=5.0.0. Dev deps: pytest>=8.3.0, pytest-asyncio>=0.24.0, httpx>=0.27.0, ruff>=0.7.0, mypy>=1.13.0. `[build-system]` with hatchling.
- `package.json`: name `@codebot/server`, private, scripts that delegate to `uv run` (build, test, lint, typecheck, migrate, dev) per Pattern 1 from research.
- `src/codebot/__init__.py`: version string `__version__ = "0.1.0"`
- `src/codebot/main.py`: Minimal FastAPI app with `/health` endpoint returning `{"status": "ok"}`. Import app for uvicorn.

**apps/dashboard/** (TypeScript, React, Vite):
- `package.json`: name `@codebot/dashboard`, private, deps: react>=18.3.0, react-dom>=18.3.0. devDeps: @types/react, @types/react-dom, @vitejs/plugin-react, vite, typescript. Scripts: dev, build, preview, typecheck (`tsc --noEmit`).
- `tsconfig.json`: strict mode with `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`, target ES2022, module ESNext, jsx react-jsx.
- `vite.config.ts`: React plugin, server port 3000.
- `src/main.tsx`: Minimal React app rendering "CodeBot Dashboard" in a root div. Include `index.html` in the dashboard root.

**apps/cli/** (TypeScript):
- `package.json`: name `@codebot/cli`, private, deps: commander. devDeps: typescript, @types/node. type: module. Scripts: build (`tsc`), dev. bin: `"codebot": "./dist/index.js"`.
- `tsconfig.json`: strict, target ES2022, module nodenext, moduleResolution nodenext, outDir dist, rootDir src.
- `src/index.ts`: Minimal CLI with commander: `codebot --version` prints version, `codebot --help` lists commands. Add shebang `#!/usr/bin/env node`.

**libs/agent-sdk/** (Python):
- `pyproject.toml`: name `agent-sdk`, python >=3.12, deps: pydantic>=2.9.0. Build-system: hatchling.
- `package.json`: Turborepo shim, name `@codebot/agent-sdk`, scripts delegate to uv.
- `src/agent_sdk/__init__.py`: Empty with docstring "Agent SDK - base classes and shared Pydantic models".

**libs/shared-types/** (TypeScript):
- `package.json`: name `@codebot/shared-types`, main dist/index.js, types dist/index.d.ts, type: module. devDeps: typescript. Scripts: build (`tsc`), typecheck (`tsc --noEmit`).
- `tsconfig.json`: strict, declaration: true, outDir: dist, rootDir: src, target ES2022, module nodenext.
- `src/index.ts`: Export placeholder comment "// Shared TypeScript types - populated in Plan 01-03".

**libs/graph-engine/** (Python):
- `pyproject.toml`: name `graph-engine`, python >=3.12. Build-system: hatchling.
- `package.json`: Turborepo shim, name `@codebot/graph-engine`.
- `src/graph_engine/__init__.py`: Empty with docstring.

**configs/**: Create `pipelines/`, `agents/`, `providers/` directories with `.gitkeep` files.

After creating all files, run `uv sync && pnpm install` to validate workspace resolution, then run `pnpm turbo build` to verify the full build pipeline works.
  </action>
  <verify>
    <automated>cd /Users/v13478/Desktop/CodeBot && uv sync && pnpm install && pnpm turbo build</automated>
  </verify>
  <done>`uv sync` installs all Python packages without errors. `pnpm install` links all workspaces. `turbo build` succeeds for all 6 workspaces. All directories exist with valid package configs.</done>
</task>

</tasks>

<verification>
1. `uv sync` succeeds from repo root
2. `pnpm install` succeeds and creates workspace links
3. `pnpm turbo build` completes for all workspaces
4. `ls apps/server apps/dashboard apps/cli libs/agent-sdk libs/shared-types libs/graph-engine` shows all directories
5. `cat pyproject.toml | grep "3.12"` confirms Python version target
6. `cat package.json` has NO next, eslint, yjs, tremor dependencies
</verification>

<success_criteria>
- Running `uv sync && pnpm install && turbo build` from a clean state completes with zero errors
- All 6 workspace packages are discoverable by Turborepo
- Python packages use uv with proper workspace membership
- TypeScript packages use pnpm with workspace protocol
- No stale/incorrect dependencies from old config files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-scaffolding/01-01-SUMMARY.md`
</output>
